# FIX RECORD
Audit ID：G06-0004
原问题：PRD 范围并列「购物车数量、创建订单」，未写订单来源。实现 `_create_order` 只扣库存、不读不改 `cart_items`。学生可能把 v1.0 写成「完成购物车结算」。
修改文件：`project/minishop/docs/PRD.md`；`project/minishop/docs/prd-coverage-matrix.md`；`chapters/19-minishop-project.md` 19.2 摘要
修改位置：R-ORDER 一行；覆盖矩阵 R-ORDER 要点；第 19 章已确认规则「创建订单」句
原内容：（摘录）

```
R-ORDER | POST /api/orders 成功返回 201 与 id，Body 不含 status。默认不幂等：两次成功 POST 得到两个 id
```

修复后内容：（摘录）

```
R-ORDER | …两次成功 POST 得到两个 id。v1.0 下单直接 POST sku+qty，不读取、不修改购物车
19.2：v1.0 下单直接 POST sku+qty，不读取购物车
```

为什么这样修：关键 P2 闭环只需冻结教学简化，不必改实现。短句即可，避免简历写成结算流程。
依据：G06-0004；`server.py` `_create_order` 不读 `cart_items`。
是否影响其他章节：未改 `server.py` / 前端。第 20 章未同步改口述（非本任务）。
验证结果：PRD 与 19.2 各一句；pytest 未因本条改变。
状态：FIXED
