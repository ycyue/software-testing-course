# FIX RECORD
Audit ID：M-P1-24（G01-0002，仅第 20 章）
原问题：收口层用现在时警告「教学服务 qty=1」，第 13–16 章现行已是 `/api/` + qty=10/11。
修改文件：`chapters/20-interview.md`
修改位置：20.3 边界；20.15 库存规则边界列；常见错误 8
原内容：（摘录）

```
边界：第 13 章教学服务不是本项目契约；无生产压测。
| 库存规则？ | … | 早期教学服务不是 v1.0 |
错误 8：把课程教学服务路径说成 MiniShop 正式契约
```

修复后内容：（摘录）

```
边界：现行即 `/api/` 与 PRD（旧 `/login` + qty=1 教学桩已删除）；无生产压测。
| 库存规则？ | … | 曾有 qty=1 教学桩，已删除；现行跟 PRD |
错误 8：把已经删掉的教学桩（`/login`、qty=1）说成现行契约
修正：现行教材第 13～16 章就是 `/api/` 与 PRD；qty=10 允许、qty=11 拒绝。不要去找第二台教学服务。
```

为什么这样修：qty=1 教学桩是已删除历史，必须过去时；现行尺子是 `/api/` 与 10/11。
依据：G01-0002；MiniShop 冻结 `/api/`、qty=10/11。
是否影响其他章节：第 22 章由 Fix-22 续修（见下方）；第 19 章与测验 6 现行正文已是过去时。
验证结果：`20-interview.md` 中「教学服务」现在时警告已消除；qty=1 仅作为已删除历史出现。
状态：FIXED（第 20 章）；全 ID 见下方第 22 章续修。

---

# FIX RECORD（续 · 第 22 章）
Audit ID：M-P1-24（G01-0002 / CH22-0002，仅第 22 章）
原问题：开篇 mermaid 漏 17/18、Linux 与 SQL 画成单线；22.1 用现在时提「各章教学服务」。
修改文件：`chapters/22-learning-path.md`
修改位置：场景导入 mermaid 与图下说明；22.1 能力链与 MiniShop 口径；常见错误 5
原内容：（摘录）

```
D --> E[Linux 与 SQL]
...
G --> H[MiniShop 面试]

贯穿项目只有 MiniShop。各章教学服务（例如第 13 章路径）在 v1.0 之前是教学约定；项目契约以 PRD 为准。
错误 5：继续用教学服务冒充 MiniShop 正式契约
```

修复后内容：（摘录）

```
D --> E11[11 Linux]
D --> E12[12 SQL]
...
G --> H[17-18 了解即可]
H --> I[19 MiniShop]
I --> J[20-22 面试求职]

上图是正式学习顺序的压缩分组，含 17、18；Linux 与 SQL 并列。

仓库里曾经有过 `/login` + qty=1 的教学桩，现行教材已删除；你读到的第 13～16 章可运行示例就是 `/api/` 与 PRD（qty=10 允许、qty=11 拒绝）。
错误 5：把已删除的教学桩或通用练习系统当成 MiniShop 现行契约
```

为什么这样修：收口章的总图必须等于正式顺序，不能另开一条漏 17/18 的课表；qty=1 教学桩是已删除历史，必须过去时。未整章重写。
依据：`docs/COURSE_OUTLINE_v1.2.md` 正式学习顺序；G01-0002；CH22-0002。
是否影响其他章节：第 19、20 章与测验 6 已改过去时（Fix-19/20）。本章 mermaid 与 22.1 与之对齐。
验证结果：章内无现在时「教学服务」；mermaid 含 17-18，11 与 12 为并行节点后汇入 13-14。
状态：FIXED

---

# FIX RECORD（续 · 第 19 章与测验 6）
Audit ID：M-P1-24（G01-0002 / G04-0010 / CH19-0001，第 19 章 + 阶段测验 6）
原问题：错误 2 / 练习 2 / 测验 6 Q5 用现在时把「第 13/16 章教学服务 qty=1」写成现行差异，学生会去找第二台服务器。
修改文件：`chapters/19-minishop-project.md`；`chapters/quizzes/stage-6-project.md`；`chapters/assets/diagrams/ch19-qty-rule.html`
修改位置：错误 2；练习 2 与答案；面试「购物车规则」边界；本章总结第 2 条；测验 6 Q5 题干与答案；qty-rule 图 title/h1/caption
原内容：（摘录）

```
错误 2：用第 13 章教学服务的 qty=1 唯一成功来测 v1.0
练习 2：v1.0 中 qty=10 与第 16 章教学服务可能不一致。
测验 6 Q5：和第 16 章教学服务可能有什么差别？
答案：早期教学服务往往只让 qty=1 成功
图题：教学服务可能只让 qty=1；第 13～16 章教学服务常只让 qty=1 成功
```

修复后内容：（摘录）

```
错误 2：拿已经删除的 qty=1 教学桩当 v1.0 期望
修正：仓库曾经有过 /login + 只让 qty=1 成功的桩，现行教材已删除。你读到的第 13～16 章就是 /api/ 与 qty=10/11，不要另找第二台服务器。
测验 6 Q5：仓库曾经有过 qty=1 教学桩，现在还要去第 16 章找第二台服务器吗？
答案：不要另找服务器，跟 PRD。现行 13～16 章已是 /api/ 与 qty=10/11。
图题：测 MiniShop v1.0 必须以 PRD 为准：qty=10 允许，qty=11 拒绝
```

为什么这样修：qty=1 桩是已删除历史，必须过去时；现行可抄示例就是仓库 MiniShop。未把错误 2 改成验证码题，按任务只改时态与「不要找第二台服务器」。
依据：G01-0002；G04-0010；MiniShop 冻结 `/api/`、qty=10/11。
是否影响其他章节：第 20、22 章已由 Fix-20/22 改过去时。示意图 PNG 需 Coordinator 按新 HTML 重截。
验证结果：第 19 章与测验 6 无现在时「教学服务 qty=1」；未改 `server.py`。
状态：FIXED
