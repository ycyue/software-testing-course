# FIX RECORD
Audit ID：M-P1-20
原问题：14.2 第一句叫人「新建 Request」，导入却在 14.6.1，线性阅读会先手建一套 `/api/login`。14.5 仍指挥「第 6 步再创建订单」，现行 15 条集合没有该请求。14.5 列表脚本只断言数组，与 14.2 自己的警告和仓库集合冲突。
修改文件：`chapters/14-postman.md`
修改位置：14.2 开篇；14.5 列表脚本；14.5 创建订单之后（删除「第 6 步」句）
原内容：（摘录）

> 新建 Request，选择方法，URL 使用变量。路径与仓库集合一致：
>
> 第 6 步“再创建订单”复用上面脚本：第一次写入 `orderId`，第二次先比较再覆盖。

列表脚本只断言 `items` 为数组。

修复后内容：（摘录）

> 先按 14.6.1 导入仓库集合和环境，再打开集合里已有的请求看面板。不要新建一套 `/api/login`。
> 打开「搜索商品」…打开「登录-正确」…

> 仓库集合只有一条「创建订单」，没有「再创建订单」。要证明 v1.0 默认不幂等：复制「创建订单」，在副本里先比较 `lastOrderId`，再 `set` 覆盖。

列表脚本断言 `items[0].sku === "SKU-DEMO-001"`（URL `keyword=鼠标`）。

为什么这样修：章首、14.7、工作实战都要求导入 15 条，不要另造 `/login`。14.2 主动作必须是打开已导入请求的面板。「第 6 步」是旧七步教案残留，14.7 已写「集合目前只下一单」。列表 oracle 必须与集合对齐。
依据：集合 `item` 恰好 15 条，无第二笔下单；14.7 L312 原口径；`practice/README.md` 14-1「导入 Postman 集合」；CH14-0003 / 0004 / 0005。
是否影响其他章节：无。未整章重写 14.2（GET/POST 语义、Headers/Body/Response 保留）。
验证结果：正文已无「新建 Request」作为本章动作、已无「第 6 步」。14.7 与 14.5 均写「复制创建订单」测不幂等。列表脚本与集合「搜索商品」同一 sku 断言。
状态：FIXED
