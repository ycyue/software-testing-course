# FIX RECORD
Audit ID：M-P1-06
原问题：06B 练习 10 标准答案把 06A / 实操 6-1 的对象写成 qty=11（库存 10 时 11 应被拒绝）。6-1 钉死的是空搜索 BUG-001（R-SEARCH）；qty=11 在 06A 是写法示范，v1.0 会拒绝 11，不要交。
修改文件：`chapters/06b-test-management.md`
修改位置：练习答案第 10 条
原内容：（摘录）
> 10. 缺陷单本身已在 6-1 交付。确认测试：在修复后的同一类构建上重放原步骤，库存 10 时 11 应被拒绝。回归测试：至少覆盖加购、修改为合法数量、修改为库存值；失败后原值是否保持以 PRD 为准，未写则待确认。v1.0 没有小计和结算，不要把它们写进 MiniShop 回归范围。
修复后内容：（摘录）
> 10. 对象是 6-1 / BUG-001（空搜索，R-SEARCH），不是 qty=11。确认测试：在修复后的同一构建上重放空关键字或仅空白关键字（接口 `GET /api/products?keyword=` 与若干空格，以及页面搜索框提交空格），按 R-SEARCH 不得把全量商品当成搜索结果（具体是 400 还是空列表+提示，以当时已确认规则为准）。回归测试：有关键字搜索仍能命中、登录仍可用、qty=10 仍允许、创建订单仍返回 id（无 status）。按修改点和风险抽测。qty=11 是另一条已实现规则（R-CART-10），不是本单确认步骤。BUG-001 在修复前不得写成已关闭。
为什么这样修：只改正文答案对象和步骤，不改题干、不重写小节。确认测的是空/空白关键字不得返回全量；回归抽测未改动的有关键字搜索、登录、qty=10、创建订单。400 与空列表不写成唯一冻结预期，与 PRD R-SEARCH 和官方 BUG-001 一致。
依据：
- `practice/06-bug-report/README.md`：作业对象空搜索 BUG-001
- `project/minishop/bugs/BUG-001.md`：R-SEARCH；空或仅空白关键字不应展示全量
- PRD `R-SEARCH` / `R-CART-10`：qty=11 是另一条已实现规则
- CH06-0001 / G04-0002 / G01-0003；FIX_PLAN M-P1-06
是否影响其他章节：同段去掉「加购」，见 M-P2-08。第 2、3 章仍可用超卖讲确认/回归概念，不在本 ID 范围。
验证结果：
- 独立再解练习 10：确认 = 修复构建上重放空/空白关键字，不得返回全量；回归 = 有关键字搜索、登录、qty=10、创建订单。与改正后答案一致。
- 答案不再把 qty=11 当本单确认步骤；题干未改。
- Markdown 完整。
状态：FIXED
