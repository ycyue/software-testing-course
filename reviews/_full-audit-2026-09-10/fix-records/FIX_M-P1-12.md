# FIX RECORD
Audit ID：M-P1-12（对应 CH08-0004；FIX_PLAN 误写 08B，实际在 08A 练习 10）
原问题：练习 10 预期写「数量保持合法值」。`05-cart-qty-11.png` 里输入框仍是 11，列表才是 `qty=1`。学生若把「输入框回到 1」当预期，会给 MiniShop 误报缺陷。
修改文件：`chapters/08a-web-page-testing.md`
修改位置：练习 10 题干；练习答案 10；工作实战第 4 条补「输入框仍显示 11 也可以」。
原内容：（摘录）
> 以及若页面显示 11 时的缺陷标题。
> 预期：提交失败，数量保持合法值，出现超限或库存不足提示。
修复后内容：（摘录）
> 以及若**商品列表**变成 11 时的缺陷标题。输入框仍显示 11 不算失败。
> 预期：提交失败；页面提示 `qty exceeds stock`（或等价）；**商品列表/接口**该 SKU 仍为提交前的 `qty=1`；输入框可以仍显示 11……不要把「数量保持合法值」写成已冻结的唯一判定。
为什么这样修：第 5 章已强调「我打不进去」≠「提交被拒绝」。可观察判定必须拆开提示、列表/接口、输入框。v1.0 不把输入框回滚写成冻结契约。
依据：`05-cart-qty-11.png`；`app.js` 失败只改 `cart-msg` 并 `refreshCart`，不把 input 改回；PRD `R-CART-10`。
是否影响其他章节：无。8.4「拒绝后显示值是否回到合法值」仍是通用观察项，不是 MiniShop 预期。
验证结果：独立再解——步骤：登录；确认教学 SKU 库存 10、列表 qty=1；改为 11 提交。预期：失败提示；列表/接口仍 1；框可仍为 11。若列表变成 11 才写缺陷。与新答案一致。
状态：FIXED
