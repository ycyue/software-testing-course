# FIX RECORD
Audit ID：M-P1-23（CH18-0001 / CH18-0002 / G05；对应 IMG-CH18-001）
原问题：`ch18-p95` 柱状图讲五个样本 100/110/120/200/800 与平均值藏不住 800 ms，caption 却整段复制 18.4「并发人数 ≠ TPS / 单账号冲突」。18.2 唯一数值例子只打印平均 266.0、中间值 120、最大值 800，没有在同一组数上算出 P95。
修改文件：
- `chapters/assets/diagrams/ch18-p95.html`
- `chapters/assets/diagrams/ch18-p95.png`（已按 `diagrams/README` 无头 Chrome 重截）
- `chapters/18-performance-testing.md`
修改位置：示意图 caption / 柱下标注；18.2 打印结果之后的百分位算例（五个样本所在节，不是 18.4 并发节）；练习 2 题干与答案；可运行性说明。
原内容：（摘录）
```
<p class="caption">示意图：并发人数 ≠ TPS。50 线程共用一个教学账号，测的是单用户冲突，不是系统容量。</p>

平均 266 ms，中间那次只要 120 ms，最慢一次 800 ms。用户感到的卡，往往在最慢的那一截。

2. 平均 266.0 ms，中间值 120 ms，最大 800 ms。只报平均会把已经很慢的 800 ms 藏起来。
```
修复后内容：（摘录）
```
<p class="caption">示意图：五个样本 100 / 110 / 120 / 200 / 800 ms。平均 266 ms 藏不住尾部。最近秩：n=5 时 5×0.95=4.75，向上取整取第 5 个，P95=800 ms；P50=120 ms。800 ms 不是 MiniShop SLA。</p>

本章约定用**最近秩**：样本从小到大排好，百分位位置取 `n × p` 再向上取整。n=5 时，P50 取第 `ceil(5 × 0.50)=3` 个，**P50=120 ms**；P95 取第 `ceil(5 × 0.95)=5` 个，**P95=800 ms**。

2. …最近秩 P50=120 ms、P95=800 ms（n=5 时 5×0.95=4.75，向上取整取第 5 个），不要把平均 266 当成「大家都还行」。
```
为什么这样修：图注必须讲本节命题（平均藏不住尾部），不能再抄 18.4 的并发≠TPS。同一组五个数上给出可复核的 P95 数字，并写明规则（最近秩、向上取整），同时声明五个点太少、线性插值约 680 ms、800 ms 不是 MiniShop SLA。算例在 18.2；18.4 只补一句数字并标明那是尾部、不是并发人数，避免再次串台。练习 2 与算例对齐。未整章重写。
依据：Apache / 工程常用最近秩 `ceil(n × p)`；n=5 时 P50=120、P95=800。Excel PERCENTILE.INC 线性插值 P95≈680，正文仅作对照，不写入 SLA。ISTQB/岗位实践用百分位描述尾部。质量标准「图表确实帮助理解」。
是否影响其他章节：否。18.4 并发图 `ch18-tps-concurrency` 未改。面试题仍用「四个 100 ms 加一个 800 ms」属 CH18-0009（P3，本轮不修）。
验证结果：
- HTML caption 已无「并发人数」「TPS」
- 柱下标注 P50=120、尾部 · P95=800
- Python 3.14：平均 266.0、中间 120、最大 800；`ceil(5×0.95)=5` → P95=800
- PNG 已重截：1320×780，`chapters/assets/diagrams/ch18-p95.png`
- 练习 2 独立再解与答案一致
状态：FIXED（PNG 已重截）
