# FIX RECORD
Audit ID：G02-0001
原问题：08B 把「未设置 SameSite」写成「现代浏览器通常按 Lax，因而挡住大多数跨站 POST」。Chrome 对未设置属性用 Lax-allow-unsafe：Cookie 写入后约 2 分钟内，顶层跨站 POST 仍可能带上。显式 `SameSite=Lax` 没有这个洞。MiniShop Cookie 未设 SameSite。
修改文件：`chapters/08b-web-auth-permission.md`
修改位置：8.11 Cookie 属性表 `SameSite` 行；紧随的 CSRF 段。
原内容：（摘录）
> 未设置时，现代浏览器通常按 `Lax` 处理；`None` 必须同时有 `Secure`
> `SameSite=Lax`（现代浏览器对未设置 SameSite 的常见默认）会挡住大多数跨站 **POST**
修复后内容：（摘录）
> 要分开三种情况：显式 `SameSite=Lax`；**未设置**（Chrome 等按更宽的 Lax-allow-unsafe，不等于显式 Lax）；`None` 必须同时有 `Secure`。MiniShop v1.0 Cookie 未设 SameSite。
> **未设置 SameSite ≠ 显式 Lax**：……大约 2 分钟内，顶层跨站 POST 仍可能带上。……不要把「默认 Lax」写成「跨站 POST 一定带不上」。
为什么这样修：学生会写两种错缺陷：（1）未设 SameSite = 已有 Lax；（2）刚登录 2 分钟内跨站 POST 带上 Cookie 却判浏览器坏了。
依据：MDN `Set-Cookie`（检索 2026-09-10）：When Lax is applied as a default, a more permissive version is used… POST … no more than two minutes。Chromium SameSite FAQ「Lax + POST mitigation」。`server.py` L299：`minishop_session=…; HttpOnly; Path=/`，无 SameSite。
是否影响其他章节：测验 3 Q10 D「Lax Cookie 能挡住一切跨站请求」仍是错项，答案仍为 C，无需改题。第 9 章 Cookie 检查清单不在本 ID。
验证结果：表与 CSRF 段已拆显式 Lax / 未设置 / None+Secure；MiniShop 脚注为未设置。
状态：FIXED
