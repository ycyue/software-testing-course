# FIX RECORD
Audit ID：M-P1-03
原问题：3.8 把 MiniShop 连续空格搜索写成「会出现异常提示」。实际是空白关键字仍返回全量商品（BUG-001 开放），页面无异常提示。读者会以为 1-1 观察错了，或把 BUG-001 理解成「有提示的异常」。
修改文件：`chapters/03-software-testing-classification.md`
修改位置：§3.8 探索性测试示例（原 L238）
原内容：（摘录）
> 例如测试 MiniShop 搜索功能时，测试人员发现输入连续空格会出现异常提示，于是继续尝试前后空格、不同语言、超长关键词和快速连续搜索。
修复后内容：（摘录）
> 例如测试 MiniShop 搜索功能时，测试人员发现输入连续空格后仍返回全量商品（空白关键字当前返回全量，BUG-001 仍开放），于是在这一观察上继续尝试前后空格、不同语言、超长关键词和快速连续搜索。
为什么这样修：只改这一句观察结果，不重写探索性定义。探索性测试的下一步仍是前后空格、不同语言、超长关键词和快速连续搜索，但起点必须是仓库真实现象：空/仅空白关键字 HTTP 200、全量三件，无异常提示。BUG-001 保持开放，与章内工作实战、第 1 章动态例子一致。未发明提示文案。
依据：
- `project/minishop/server.py`：`keyword` 为空或 `strip()` 后为空时仍返回全列表；注释写明 BUG-001
- `project/minishop/bugs/BUG-001.md`：实际 HTTP 200，三件商品；预期按 R-SEARCH 不应展示全量
- 第 1 章：动态、仓库现状是返回 3 件，BUG-001 仍开放
- 本章工作实战：空白关键字搜索不应返回全量（当前实现不符合，见 BUG-001）
- G01-0005；MASTER D2；FIX_PLAN M-P1-03
是否影响其他章节：否。未改 BUG-001 报告、第 1 章或 08A。3.8 现与那些口径对齐。
验证结果：
- 章内「异常提示」已无；空白/空搜索只出现在 BUG-001 全量返回语义（3.8、工作实战、练习 8）。
- 探索性步骤仍接在真实观察之后，未写成已修复，也未虚构提示。
- Markdown 一句完整。
状态：FIXED
