# 阶段 5 复审：第 13～16 章（含 15A/15B、16A/16B）

- 日期：2026-09-09
- 角色：课程质量审查员（只读正文，未改教材源文件）
- 范围：`chapters/13-api-testing.md`、`14-postman.md`、`15-python-basics.md` + 15A/15B、`16-pytest.md` + 16A/16B；`practice/13-api-shapes/`、`15-json-check/`、`16-pytest-regression/`；`project/minishop/postman/`、`project/minishop/tests/`、`project/minishop/docs/PRD.md`、`openapi.json`（抽样）；`chapters/quizzes/stage-5-api.md`
- 依据：Quality Standard v1.0、PRD v1.0 冻结口径、本轮任务书（旧 `reviews/` 不当真理）
- 核心章目标：第 13 / 14 / 16 章 ≥95；最低发布 90

旧分对照（v1.2.1，仅作基线，不继承结论）：13=91，14=91，15 上/下=94/94，16 上/下=94/95。

---

## 总判

**不通过。** 第 13 / 14 / 16 章均未达核心目标 95；第 13、14、16B 掉到最低发布线 90 以下。阶段测验与仓库 pytest 基线本身是诚实的，但**双基线已经散成可执行的 spaghetti**：学生从第 1～12 章实操打的是 v1.0 `/api/`（`qty=10` 允许、购物车要 Bearer），第 13～16 章正文主示例却是另一台教学服务（`POST /login`、常只让 `qty=1` 成功、购物车不校验认证）。标签写了，练习仍会抄错契约。

**judo 可行，而且应该做：** 教学示例直接改打仓库 MiniShop v1.0，删掉 `teach_server.py` 和 14.7 那套 `/login` 表。第 19 章之前只需一句「这是仓库当前实现，与 PRD 一致；冻结仪式在第 19 章」，不必维持第二套 HTTP 契约。

仓库数字核过：`project/minishop/evidence/pytest-output.txt` 为 **37 passed, 1 xfailed**（BUG-001）。Postman GUI / Collection Runner：**作者未点**，第 14 章可运行性说明未撒谎。

| 章 | 本轮 | 旧分 | 目标 | 发布 |
| --- | ---: | ---: | ---: | --- |
| 13 | **88** | 91 | 95 | 不得发布（核心章未达标，且低于 90） |
| 14 | **87** | 91 | 95 | 不得发布 |
| 15A | **92** | 94 | 90 | 可发布（非核心） |
| 15B | **91** | 94 | 90 | 可发布 |
| 16A | **90** | 94 | 95 | 卡在最低线，未达核心目标 |
| 16B | **88** | 95 | 95 | 不得发布 |
| 测验 5 | 通过（概念） | — | — | 不救正文双基线 |

第 13 章「91 的缺口还在不在」：**在，而且比 91 所写的「双路径标签」更重。** 91 把它当成声明问题；本轮看到的是**学生按正文 curl 打唯一会开的服务会 404，按 13-1 又学到另一套契约。**

---

## 各章评分

评分按 Quality Standard §九。DoD 未全过的章在表下说明。

### 第 13 章 接口测试 — 88/100

| 项目 | 满分 | 得分 |
| --- | ---: | ---: |
| 技术准确性 | 25 | 22 |
| 知识完整性 | 15 | 15 |
| 初学者友好 | 15 | 12 |
| 实战价值 | 15 | 13 |
| 示例质量 | 10 | 8 |
| 项目一致性 | 5 | 2 |
| 面试价值 | 5 | 4 |
| 练习质量 | 5 | 4 |
| 结构与表达 | 5 | 3 |
| **总分** | **100** | **88** |

DoD：**17/20**（失败项：3 无知识性错误、10 MiniShop 一致、11 代码经过验证）。17 及以下不得发布。概念（REST≠JSON、四态、401/403、POST 默认不幂等、订单无 `status`）仍然对；失败在**可执行契约**。

缺口相对 91：**仍在。** 新增确认：实操 13-1 打 v1.0，正文 curl 打的教学服务在第 13 章仓库里**没有可启动文件**。

### 第 14 章 Postman — 87/100

| 项目 | 满分 | 得分 |
| --- | ---: | ---: |
| 技术准确性 | 25 | 23 |
| 知识完整性 | 15 | 14 |
| 初学者友好 | 15 | 11 |
| 实战价值 | 15 | 12 |
| 示例质量 | 10 | 7 |
| 项目一致性 | 5 | 2 |
| 面试价值 | 5 | 5 |
| 练习质量 | 5 | 4 |
| 结构与表达 | 5 | 4 |
| **总分** | **100** | **87** |

DoD：**17/20**（失败：10 MiniShop 一致、7 零基础可理解、8 示例具体——同一章两套集合）。Environment 覆盖 Collection 的口径正确；GUI 未点已披露。扣分主因是 **14.6.1 导入 v1.0 集合，14.7 工作实战却要学生手建 `/login` 教学七步**，后者在第 14 章没有可启动的对端。

### 第 15 章（上）Python 语法与数据 — 92/100

| 项目 | 满分 | 得分 |
| --- | ---: | ---: |
| 技术准确性 | 25 | 24 |
| 知识完整性 | 15 | 14 |
| 初学者友好 | 15 | 13 |
| 实战价值 | 15 | 13 |
| 示例质量 | 10 | 9 |
| 项目一致性 | 5 | 4 |
| 面试价值 | 5 | 5 |
| 练习质量 | 5 | 4 |
| 结构与表达 | 5 | 5 |
| **总分** | **100** | **92** |

DoD：**18/20**（弱项：工作实战下放到 15B；练习编号跳号）。未讲 class/装饰器/继承/异步；`True` 是 `int` 子类只作测试陷阱；未安装 requests/pytest。这些冻结项**通过**。

### 第 15 章（下）模块、文件与 JSON — 91/100

| 项目 | 满分 | 得分 |
| --- | ---: | ---: |
| 技术准确性 | 25 | 24 |
| 知识完整性 | 15 | 14 |
| 初学者友好 | 15 | 13 |
| 实战价值 | 15 | 14 |
| 示例质量 | 10 | 8 |
| 项目一致性 | 5 | 3 |
| 面试价值 | 5 | 5 |
| 练习质量 | 5 | 4 |
| 结构与表达 | 5 | 4 |
| **总分** | **100** | **91** |

DoD：**18/20**。工作实战 `cart_cases.json` 按业务规则让 `qty=10` 为 `ok`（对）；配套实操 15-1 却对 v1.0 发 HTTP，与 15A「不发 HTTP / 发 HTTP 是第 16 章的事」打架。

### 第 16 章（上）pytest 基础 — 90/100

| 项目 | 满分 | 得分 |
| --- | ---: | ---: |
| 技术准确性 | 25 | 23 |
| 知识完整性 | 15 | 14 |
| 初学者友好 | 15 | 13 |
| 实战价值 | 15 | 14 |
| 示例质量 | 10 | 8 |
| 项目一致性 | 5 | 3 |
| 面试价值 | 5 | 5 |
| 练习质量 | 5 | 4 |
| 结构与表达 | 5 | 4 |
| **总分** | **100** | **90** |

DoD：**18/20**。ROI、收集规则、`timeout`、不对 400 先 `raise_for_status`、xfail 口径都对。仓库 37/1 与证据一致。未达 95，因为**主示例仍是 `/login` + qty=1-only**，却把学生往 `python3 run.py test`（v1.0）送。

### 第 16 章（下）fixture 与 MiniShop 自动化 — 88/100

| 项目 | 满分 | 得分 |
| --- | ---: | ---: |
| 技术准确性 | 25 | 21 |
| 知识完整性 | 15 | 14 |
| 初学者友好 | 15 | 12 |
| 实战价值 | 15 | 13 |
| 示例质量 | 10 | 7 |
| 项目一致性 | 5 | 2 |
| 面试价值 | 5 | 5 |
| 练习质量 | 5 | 4 |
| 结构与表达 | 5 | 5 |
| **总分** | **100** | **88** |

DoD：**17/20**（失败：3 知识性错误——场景写 `qty=1/10/11` 展开，代码与 `teach_server` 对 `qty=10` 返回 400；10 MiniShop 一致）。autouse 口径符合冻结（单 autouse ≠ 测不到 401，共享 Session/Cookie 才会测脏）。教学下单与教学购物车都只接受 `qty=1`，**这一条相对旧审计已对齐**；但场景句把 `qty=10` 又写回去了。

---

## Issues

级别：HIGH = 学生按正文做会打到错误契约或无法执行；MEDIUM = 结构/答案/索引缺口，能绕过但会混；LOW = 表达或模板。

| ID | 级 | 位置 | 问题 |
| --- | --- | --- | --- |
| S5-01 | HIGH | `chapters/13-api-testing.md:332-367` `13.12`；同文件 `:386-396` `13.13`；`:633-640` 可运行性说明 | 正文 curl 是 `POST /login`、`POST /orders`、`POST /cart/items`。v1.0 `server.py:248-260` **只有** `/api/login`、`/api/orders`、`/api/cart/items`。学生按第 1～12 章习惯执行 `python3 project/minishop/run.py serve`（8765）再抄本章 curl → **404**。第 13 章仓库**没有** `teach_server.py`；该文件到 16B 才以 class 墙出现。审查声称已 curl 的教学服务，学习者无法复现。 |
| S5-02 | HIGH | `practice/13-api-shapes/main.py:38-47` vs `13-api-testing.md:246-259` `:403` | 实操 13-1 打 **v1.0**：`/api/login` + Bearer + `/api/cart/items`，错误信息 `missing qty` / `null qty` / `wrong type qty`。正文 13.8 与工作实战写「教学服务或授权环境」，四态表还夹着「`qty=10` 按规则应允许，教学服务未实现」。同一章两条可执行路径。`practice/README.md:84` 已承认「实操一律打 v1.0」，正文没把 13-1 标成「不要抄上面的 `/login`」。 |
| S5-03 | HIGH | `13-api-testing.md:508` 面试「先在接口测 10 和 11」；`:251-259` 13.8 | 面试示例按 R-CART-10。教学服务 `qty=10` 不是 200。学生若用面试句当断言，对着教学服务会误报缺陷；对着 MiniShop 才对。已标明教学约定**不能**放过这条。 |
| S5-04 | HIGH | `16b-pytest-fixtures.md:222-300` `teach_server.py` 购物车分支 | `qty == 11` → 400；`qty == 1` → 200；**else（含 10、缺失、null、`""`、`"1"`）一律 `bad qty`**。第 13 章要求四态分开观察；教学服务**不能**区分缺字段与 null。16.9 parametrize 只断言状态码 400，掩盖了这一点。抄到 v1.0 会得到另一套 `error` 字符串。 |
| S5-05 | HIGH | `16b-pytest-fixtures.md:24` vs `:135-154` `:421-439` vs `teach_server` `:277-285` | 场景：「parametrize 把 **qty=1/10/11** 和缺字段四态展开」。代码表只有 `qty=1`→200 与 `qty=11`→400，**没有 qty=10**。若学生按场景补 `qty=10` 期望 200，教学服务返回 400。这是本章内自相矛盾，不是「已标明的教学简化」。 |
| S5-06 | HIGH | `16a-pytest-basics.md:203`；`16b:9` `:212-490` vs `practice/16-pytest-regression/` | 16A：「第 19 章 MiniShop v1.0 **才**按 PRD 让 `qty=10` 通过」。假。实操 5-1 / 13-1 / 16-1 和 `tests/test_api.py:75` 现在就让 `qty=10`→200。16B 工作实战要学生复制教学包跑 **16 passed**；16-1 跑仓库 **37/1**。两套 pytest，路径/认证/数量规则都不同。 |
| S5-07 | HIGH | `16b-pytest-fixtures.md:147-153` `:433-438` 购物车测试不带 Bearer；`:158` 承认教学服务未校验购物车认证 | 抄到 v1.0：`test_cart_unauthorized` 证明无 Bearer → **401**（`test_api.py:120-126`）。教学绿的购物车六条在 MiniShop 上会全 401。标签在，练习仍会抄错。 |
| S5-08 | HIGH | `14-postman.md:267-285` 14.6.1 vs `:295-310` 14.7 vs `:317-332` 工作实战 | 14.6.1：导入 `project/minishop/postman/`，`baseUrl=http://127.0.0.1:8765`，路径 `/api/`，含注册、`qty=10`、越权。14.7 + 工作实战：手建 `GET /products?keyword=mouse`、`POST /login`、购物车只有 1 和 11、订单 `SKU-DEMO-001` `qty=1`、再下一单。**完成标准绑在 14.7**。第 14 章没有教学服务可启动。学生要么 14.7 对着 MiniShop 404，要么以为导入集合就算完成工作实战。 |
| S5-09 | HIGH | `14-postman.md:87` `:301` vs 集合 `MiniShop.postman_collection.json:62` vs `server.py:100,269-271` | 教学搜索 `keyword=mouse`；v1.0 商品名是「无线鼠标」，集合用 `keyword=鼠标`。过滤是 `name`/`sku` 的大小写包含：`"mouse"` **匹配不到**「无线鼠标」或 `SKU-DEMO-001`，返回空数组。14.7 断言只要求 `items` 为数组，空数组也绿，学生会以为搜到了。 |
| S5-10 | MEDIUM | `14-postman.md:84-92` `:119` 教学 `8080` vs v1.0 `8765` | 端口也双份。Environment 例子写教学常用 8080，v1.0 环境文件是 8765。选错环境再叠加选错路径。 |
| S5-11 | MEDIUM | 集合 `MiniShop.postman_collection.json:184-196` vs 14.7 第 5～6 步 | 集合创建订单 Body 是 `SKU-DEMO-003` `qty=1`，且**没有**「再创建订单」不幂等步。14.7 用 `SKU-DEMO-001` 并要求第二次不同 `id`。集合与正文请求表不一致（sku 选择、是否测幂等）。`sku` 字段本身两边都有，旧 CH14-缺 sku 已修。 |
| S5-12 | MEDIUM | 集合 `:9-51` 注册三条；14.7 无注册 | 集合正确落实 R-REG：201 + `phone`、无 `token`；非法 400；占用 409。14.7 / 第 13 / 16 教学包完全不教注册。14.6.1 顺序列了注册，工作实战又不要求。学生不知道哪份是「本章要交的集合」。 |
| S5-13 | MEDIUM | `15a-python-syntax.md:25` `:700` vs `practice/15-json-check/main.py:27-28` | 15A：「发 HTTP 是第 16 章的事」「本章不发 HTTP」。15-1 对 v1.0 `GET /api/products` 发 HTTP（标准库，未装 requests，冻结「不装 requests」形式上过）。学生读 15A 会以为 15-1 越界，或反过来以为正文撒谎。 |
| S5-14 | MEDIUM | `15a-python-syntax.md:636-684`；`15b-python-files-json.md:528-558` | 练习编号被拆成 1/3/4/5/6/8 与 2/7/9/10，索引页未说明。15A 练习 4 在尚未讲 `json` 模块时使用 JSON 字面 `{"qty": null}`（Python 里是 `NameError`）。答案本身不自相矛盾，但上半章无法在 REPL 验证。 |
| S5-15 | MEDIUM | `15-python-basics.md`、`16-pytest.md` 索引；15A/16A 缺「工作实战」「进入下一章自测门槛」 | 拆章后索引能分流，链接不断。15A/16A 缺质量标准要求的工作实战与自测门槛（下放到 B）。15B 标题下重要级别丢失；`15b:209-211` 星级掉到配图下一行。16A 有核心目标 95，16B 头信息更薄。 |
| S5-16 | MEDIUM | `16b-pytest-fixtures.md:222-312` | 教学服务是一整份 `BaseHTTPRequestHandler` 子类。15/16 冻结「不讲类」；16B 声明「不必会写 class」，但工作实战的启动方式就是复制这份 class。零基础执行成本高，而 `python3 run.py serve` 已经存在。 |
| S5-17 | LOW | `14-postman.md:545-552` 可运行性说明「校验教学 Collection」 | 仓库里只有 v1.0 集合（`info.name` 还叫 “MiniShop v1.0 Teaching”）。14.7 七请求没有入仓 JSON。审查校验的是哪一份，读者分不清。 |
| S5-18 | LOW | `quizzes/stage-5-api.md` | 测验本身正确：Q3 以 PRD/OpenAPI 为准、Q4 Environment 覆盖 Collection、Q5 不要用已登录 Session 测 401、Q8 ROI 非永远最高、Q10 B。它**测到了**双基线，但不能让正文变得可执行。Q5 把 autouse 与共享 Session 写在同一答案里，与 16B 细口径相比略挤，未写反。 |
| S5-19 | LOW | `13-api-testing.md:176-209` OpenAPI 教学片段 `paths./login` | 标明 `0.0.0-teaching`。对照 `openapi.json:9` 正式路径是 `/api/login`。作为「怎么读 OpenAPI」可以留；作为「MiniShop 文档」会污染第 19 章前的记忆。judo 后应改成抽样正式片段。 |

未发现、或确认已对齐（不记为 Issue）：

- 订单成功 Body 无 `status`：教学与 v1.0 一致（`13:386`、集合 `:180-186`、`test_api.py:140`）。
- R-PERM：集合「越权-他人订单 / 管理员读明细」均 403；`test_api.py:164-185` 同。管理员不走 `GET /api/orders/{id}` 看他人订单。
- R-AUTH：v1.0 登录 JSON `token` + `Set-Cookie`；后续测试用 Bearer。
- pytest 基线 37/1 与 `evidence/pytest-output.txt`、16-1 验收一致。
- Postman 未点 GUI：`14:271`、`practice/STATUS.md:35` 诚实。
- Environment 覆盖 Collection：`14:143` 正确。
- autouse：`16b:59` 符合冻结（还要 Session/Cookie 才会测脏）。
- 教学下单与教学购物车都只接受 `qty=1`（`16b:277-297`），旧「一边 qty=1-only 一边下单 qty=10」**已修**；新问题是场景句又写回 1/10/11。
- 第 15 章未安装 requests/pytest。
- 登录**页**未写成 `/login`（本章 `/login` 都是 API 路径）。v1.0 页面仍是 `/` 与 `/admin.html`。
- 小练习 13 与 16 的答案内部不互殴；15A/15B 答案互补。真正互殴的是**正文场景 vs 代码 vs 实操对端**。

---

## 结构简化机会（双基线能否消灭）

**能消灭。建议 judo，不要继续加免责声明。**

现状不是「第 19 章前故意用教学约定」的干净分层，而是：

| 层 | 实际打谁 |
| --- | --- |
| 实操 1-1～13-1、15-1、16-1 | MiniShop v1.0 `/api/`，`qty=10` 允许，购物车要 Bearer，密码 `Test1234` |
| 第 13 章 curl / OpenAPI 片段 / 工作实战 | 审查机上的教学 HTTP，`/login`，密码 `<redacted>`，第 13 章无启动文件 |
| 第 14 章 14.6.1 | v1.0 集合 + `run.py serve` |
| 第 14 章 14.7 + 工作实战 + 练习 10 | 教学七请求，无对端 |
| 第 15 章工作实战 | 本地 JSON，规则按 `qty<=stock`（含 10=ok），不发 HTTP |
| 第 16 章工作实战 | 复制 `teach_server.py`，16 passed，购物车无认证 |
| 第 16 章 `run.py test` / 16-1 | 37/1，v1.0 |

学生无法「选一条路走完」。免责声明越多，抄错概率越高。

**judo 方案（推荐，一次改掉 S5-01～S5-12、S5-16）：**

1. 第 13～16 章所有 HTTP 示例改为仓库 MiniShop：`POST /api/login`、`POST /api/cart/items`、`POST /api/orders`，端口 8765，启动命令 `python3 project/minishop/run.py serve`。
2. 购物车规则与 PRD / 实操 5-1 一致：`qty=10`→200，`qty=11`→400；Body 必带 `sku`；需要 Bearer。
3. 四态错误信息与 13-1 / `server.py:366-382` 对齐（`missing qty` / `null qty` / `wrong type qty`），不要再用教学 `bad qty`。
4. 删除 16B `teach_server.py` 整墙；第 16 章工作实战改为「读懂并改 `project/minishop/tests/` 的一条」，或从该目录复制 subset，而不是第二套 16 passed。
5. 第 14 章只保留 14.6.1 集合。14.7 表改成对该集合的阅读/补断言（补上「再创建订单」若不幂等仍要教）。搜索关键字与集合一致用「鼠标」，不要 `mouse`。
6. 注册：要么第 14 章正式教 R-REG（集合已有），要么从 14.6.1 建议顺序拿掉注册并说明「第 19 章再收」。不要一半有一半无。
7. 第 19 章之前保留**一句**标签：「当前实现与 PRD 一致；项目收口与冻结仪式在第 19 章」。质量标准 §五要的是「别假装第 13 章已经举行冻结仪式」，不是「必须另造 `/login` 服务器」。
8. 第 15 章继续不装 requests/pytest。15-1 若保留 HTTP，改写成「用标准库读一次真实 `/api/products`，为第 16 章做形状预习」，与 15A「不发 HTTP」二选一，不要并存。

**不要做的「假简化」：** 再加第三段「千万别和 v1.0 搞混」。声明已经够多（13 错误 10、14.2、14.6.1 末句、16A 错误 5、16B 错误 2）。

拆章索引本身可留：`15-python-basics.md` / `16-pytest.md` 分流、12B→13→14→15A→15B→16A→16B→17、测验 README 列出 15A/15B/16A/16B，**链接完整**。需要补的是：索引上注明「工作实战在 B」；15A/16A 练习编号连续或注明「题号跨上下册」；15B 头信息补重要级别。

---

## 可接受风险

下列本轮**不**当作发布阻塞，但要保持诚实，不得写成已完成：

1. **Postman GUI / Collection Runner 作者未点。** `14:271`、`practice/STATUS.md:35` 已披露。judo 后仍是读者练习，仓库不得补假截图。
2. **pytest / requests 补丁号会变。** 16A 写 pytest 9.1.1 是审查机快照，原理 API 稳定即可。
3. **第 15 章不讲 class。** 若 judo 删掉 `teach_server`，这个风险自然消失；若暂留教学服务，必须继续写「当作机器，不必会写 class」。
4. **15-1 用标准库发 HTTP、不装 requests。** 若选择保留，这是预习而非破戒；必须改 15A 那句「发 HTTP 是第 16 章的事」。
5. **空搜索 BUG-001。** 集合与 pytest xfail 一致，Runner 会红一条。已说明。不要改成全绿。
6. **质量标准 §五「第 19 章前不写成已冻结」。** judo 后用「当前实现 / 收口在 19」一句即可，不必恢复第二套 API。
7. **阶段测验 5** 作为读后检查可用；不能替代把正文对端改成 MiniShop。

---

## 交接

给修正执行者（仍不要在本轮改教材，除非主控另开任务）：

**先做（阻塞 13/14/16B 发布）：**

1. 选定单一 HTTP 对端 = MiniShop v1.0。改第 13 章 curl、OpenAPI 教学片段、13.8 表（`qty=10` 断言 200）、面试「测 10 和 11」。13-1 与正文合一。
2. 第 14 章删除或改写 14.7 教学七步，工作实战改为 14.6.1 集合；补不幂等第二次下单或明确「本集合用耳机 SKU 只下一单，幂等在 pytest」。搜索关键字与集合对齐。
3. 第 16 章：删 `teach_server`；parametrize 与 `tests/test_api.py:71-84` 对齐（含 `qty=10`、Bearer）；改掉 16B:24 的 `qty=1/10/11` 或真的加上 `qty=10`→200。16A:203 删掉「第 19 章才让 qty=10 通过」。
4. 16-1 继续 37/1；工作实战不要再承诺另一套 16 passed。

**可并行：**

5. 15A/15B：统一「本章是否发 HTTP」；练习题号说明；15.12 星级归位。
6. 索引页补「实战在下半章」。
7. 正式 OpenAPI 抽样进第 13 章，替换 `/login` 片段。

**验收（修正后再审）：**

- 只启动 `python3 project/minishop/run.py serve`，第 13 章每条 curl、第 14 章工作实战每条请求、第 16 章工作实战每条 pytest 都能按正文期望通过（空搜索 xfail/红条除外）。
- 全文搜索第 13～16 章：教学 `POST /login`（无 `/api`）、`qty=1-only`、`teach-token`、`teach_server` 不再作为可抄示例（历史对比可以留一句）。
- 购物车示例带 `sku`；`qty=10` 与 `qty=11` 成对且与 PRD 一致；下单 Body 无 `status`。
- 核心章回评 ≥95，且 DoD 不再在「MiniShop 一致 / 代码经过验证」上失败。

**不要做：** 为了保 91 分再堆「这是教学约定」段落；不要把 13-1 改回教学服务（实操目录已经全是 v1.0）。
