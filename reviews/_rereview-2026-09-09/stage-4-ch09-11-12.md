# 阶段 4 独立复审：第 9 / 11 / 12（含 12A/12B）章

- 日期：2026-09-09
- 审查员角色：课程质量审查员（只读正文与证据，未改教材源文件）
- 依据：`standards/QUALITY_STANDARD_v1.0.md`、`project/minishop/docs/PRD.md`、本轮冻结口径
- 旧 `reviews/chapter-09|11|12-review.md` 与 `full-course-audit-v1.2.md` 仅作对照，不作为结论来源
- 范围：`chapters/09-computer-network-and-http.md`（806 行通读）、`11-linux.md`、`12-database-and-sql.md` + `12a` + `12b`；`practice/09-http-observe/`、`11-log-grep/`、`12-sql-cross-check/`；`project/minishop/evidence/http|linux|sql|logs/`；`chapters/quizzes/stage-3-web.md`（HTTP）、`stage-4-ops.md`

---

## 总判

冻结口径整体命中，本轮**没有**发现仍在教授「GET 不安全 / POST 安全」「safe = 可缓存」「登录 POST 一定两笔订单」「302 一定变 GET」「127.0.0.1 绝对不会出网」「macOS `free` 可当 Linux 证据」「SQLite 有 TRUNCATE」「Linux 是 SQL 硬前置」「教学库 `13800138002` 是管理员」这类硬错误，也**没有**发现把未执行的远程 Linux 主机输出写成已跑完。

三章都站在脊柱「测试 = 观察 + 判定 + 证据」上：第 9 章补 HTTP 四格，第 11 章补日志/进程/磁盘，第 12 章补库状态。实操 9-1 / 11-1 / 12-1 与冻结任务对齐（`/api/login`、qty=11 + `inventory reject`、JOIN 核对购物车）。

未达「可以放手」的原因不是原理写错，而是三处教学结构债：

1. 第 9 章 806 行未拆，动手点（四格 / 实操 9-1）要读到 9.13 才出现；同一章并存 `/login`、`/api/login`、第 13–16 教学服务与第 19 章冻结四条路径信息。
2. 第 11 章开篇把 MiniShop `qty=11` 写成 HTTP 500，教学日志写成 `ERROR`，而仓库证据与实操是 **400 + `INFO inventory reject`**。
3. 第 12 章目标 95，本轮 **93**。SQL 对、种子有 INSERT、002≠099 已标明；扣分在 12A 结构薄、INSERT 节未重复写安全警告、12B 把实操 12-1 误绑到 qty=11。

发布建议：第 9、11 章属「18～19 项，修正后发布」；第 12 章未到核心章 95，先修 Issues 中的中项再复评。

---

## 各章评分

质量标准分项：技术准确性 25、知识完整性 15、初学者友好 15、实战价值 15、示例质量 10、项目一致性 5、面试价值 5、练习质量 5、结构与表达 5。发布线 90；第 12 章目标 95。

| 章 | 总分 | 技术 | 完整 | 初学 | 实战 | 示例 | 项目 | 面试 | 练习 | 结构 | 相对目标 |
| --- | ---: | ---: | ---: | ---: | ---: | ---: | ---: | ---: | ---: | ---: | --- |
| 9 HTTP | **91** | 24 | 15 | 12 | 14 | 8 | 4 | 5 | 5 | 4 | 非 95 章；过发布线，结构债未清 |
| 11 Linux | **91** | 24 | 15 | 13 | 14 | 8 | 3 | 5 | 5 | 4 | 过发布线；MiniShop 场景码写错 |
| 12 SQL（A+B） | **93** | 24 | 14 | 14 | 14 | 9 | 5 | 5 | 4 | 4 | **未到 95** |

### 第 9 章 91 — 分项说明

- **技术 24/25：** RFC 9110 safe / idempotent / cacheable 分开（`09-computer-network-and-http.md:286-287`、`:341`）；PATCH 标 RFC 5789 且不在 9110 方法表（`:302`）；登录重复提交与下单两笔订单分开（`:306`）；302 不必然改 GET，并区分 303/307/308（`:387-389`、`:495`）；`127.0.0.1` 只写回环=本机，并给出 MiniShop 本机部署例外（`:98`），**没有**「绝对不会出网」。QUERY 写作 RFC 10008 与 2026-06 现行文本一致，不扣。余 1 分给 9.7 表把 `/login` 当「登录场景例子」却不标教学/未冻结（见 Issues）。
- **完整 15/15：** 地图、TCP/UDP、TLS、报文、方法、状态码、头、体、观察记录、常见错误、面试、10 题均在。
- **初学 12/15：** 开篇阅读提示已承认「IP/TCP/TLS 第一次只需地图」（`:5`），等于承认正文人太长；四格要到 9.13（`:464`）和实操入口（`:544`）。
- **实战 14/15：** 实操 9-1 打 `POST /api/login`，断言 token + `Set-Cookie`，与 PRD `R-AUTH` 一致。工作实战仍写「若 MiniShop 尚未提供可运行服务」（`:548`），与仓库已有 `project/minishop` 及 9-1 可跑矛盾。
- **示例 8/10：** 示例 A/B 可读，密码占位正确；A 用 `/login`+302，B 用 `/api/login`+200 且响应无 `Set-Cookie`，和 9-1/R-AUTH 的「两类凭证」不完全同构。
- **项目 4/5：** 示例 B 已标明第 19 章才冻结（`:499`），符合冻结口径；9.7 表未跟同一纪律。
- **面试 / 练习 5+5：** GET/POST、401/403、幂等、练习 3 必选 C，均打在禁止绝对化上。
- **结构 4/5：** 13 块模板齐全，有一句话核心；文件过大、动手点过晚，见结构简化。

DoD：约 **18/20**（第 7 项零基础、第 10 项 MiniShop 路径标签、第 13 项图表存在但救不了篇幅）。修正后可发布。

### 第 11 章 91 — 分项说明

- **技术 24/25：** macOS 无 GNU `free` 同样输出（`:14`、`:351`、练习 9）；curl 占位 `PORT`/`/login` 已标明 v1.0 是 `8765/api/login`（`:415`、`:435`）；`kill -9` / `chmod 777` / `rm -rf` / `curl | bash` 边界清楚。余 1 分：开篇把课程主案例 qty=11 写成 500，属于对 MiniShop 行为的错误陈述，不完全是 Linux 知识错误。
- **完整 15/15：** 目录、日志、grep/find、管道、进程、权限、磁盘、ssh/scp、curl 全覆盖。
- **初学 13/15：** 命令清单长但线性；教学日志 `ERROR` 会让学生按正文 `grep ERROR` 去打 evidence，打空。
- **实战 14/15：** 实操 11-1 先 `qty=11` 再 grep `inventory reject`，与冻结任务一致；书面排障包 + `evidence/linux/*` 可对照。
- **示例 8/10：** 教学日志时间/级别与 `evidence/logs/app-sample.log` 不一致（已声明「不是正式日志格式」，仍是陷阱）。
- **项目 3/5：** sku/qty/用户与 PRD 一致；**场景状态码与级别不一致**是本章最大扣分。
- **面试 / 练习 5+5：** grep vs find、`free`、脱敏 curl 都在必会集。
- **结构 4/5：** 模板完整；不像第 9 章有「先读哪几节」；实操链接在 `:458`，偏晚。733 行作为命令目录可接受，不强制拆。

DoD：约 **18/20**（第 3/10 项受 500 场景拖累）。修正场景与日志级别后可发布。

诚实证据：**未虚构真实 Linux 主机。** `:715-717` 写明审查主机若为 macOS、`ssh`/`top`/`kill`/`journalctl` 不冒充已对 MiniShop 生产执行。`evidence/linux/df.txt` 是 APFS/`/dev/disk3s5`，`pwd-ls.txt` 是作者本机路径，`curl-login-headers.txt` 是 Python `BaseHTTP/0.6` 的 HTTP/1.0——与「本机真实输出」声明一致。

### 第 12 章 93 — 分项说明（目标 95）

按枢纽 + 12A + 12B 合分。12 枢纽页（12 行）只起目录作用，与第 8 章拆法同型。

- **技术 24/25：** `NULL`/`IS NULL`、JOIN、`COUNT(*)` vs `COUNT(列)`、SQLite 无 TRUNCATE（`12b:113`）、ROLLBACK 因引擎/autocommit 而异（`12b:113`、`:131`、`:257`）、Linux 非硬前置（`12a:23`）均正确。种子声称有 INSERT（`12a:109-122`）。余 1 分：12.12 INSERT 节未把「授权教学库 + 先 SELECT」贴在第一条写语句上；12B 场景把 12-1 说成 qty=11 核对（`12b:24`），与脚本 qty=2 不符。
- **完整 14/15：** 查询/写/事务/MiniShop 核对都在；12A 工作实战只有 3 句（`12a:366-368`），没有 12B 那种书面包。
- **初学 14/15：** 两套库（教学 `13800138002` / NULL vs v1.0 管理员 `13800138099`）反复标明（`12a:125`、`:382`），负担仍在但不是没写。
- **实战 14/15：** 实操 12-1 是登录后改 qty=2 再 JOIN，符合冻结「SQL JOIN 核对购物车」；qty=11 不落库在 `evidence/sql/seed-join.txt` 与 `test_cart_qty_11_does_not_persist`。场景误绑 12-1 扣实战分。
- **示例 9/10：** 12.4 三表数字自洽（COUNT 3/2、qty_sum=4、AVG=6.0、Tester A 鼠标 1/10 键盘 2/5），与 evidence 第一节同构（用户 3 不同，正文已禁对答案）。
- **项目 5/5：** 手机号/SKU/库存 10、不编订单 status、002≠099，均对齐 PRD。
- **面试 5/5：** UI 对库不对仍是缺陷；TRUNCATE；先 SELECT。
- **练习 4/5：** 12A 仅练习 1–5；12B 练习 7–8 是 LEFT JOIN / 无 ORDER BY 的 LIMIT，属 12A 残留。
- **结构 4/5：** 拆 A/B 是对的 judo；12A/12B 都没有「进入下一章的自测门槛」（第 9、11 章有）。

DoD：约 **18/20**（第 12 项 SQL 安全对 UPDATE/DELETE 强、对 INSERT 弱；第 17 项练习覆盖偏薄）。**未到 95，不建议按核心章放过。**

---

## Issues

级别：高 = 会教错或让学生按正文操作失败；中 = 与冻结/PRD/证据冲突或明显结构债；低 = 摩擦、标签缺口、残留。本轮无「高」知识性错误。

### 第 9 章

| ID | 级别 | 位置 | 问题 | 为何独立于旧 review |
| --- | --- | --- | --- | --- |
| S4-09-01 | 中 | `09-…md` 全章 806 行；动手点 `:464`、`:544` | 单文件同时承担网络地图、TCP/TLS、方法语义、状态码百科、头/体、登录报文。阅读提示（`:5`）已让初读跳过 IP/TCP/TLS，说明作者知道一次读不完。对照：第 8 章 8A 425 + 8B 422，第 12 章 12A 469 + 12B 355。 | 旧审查纠 PATCH 归属，未评篇幅。 |
| S4-09-02 | 中 | `:257`、`:471`、`:499-502`；`practice/09-http-observe/README.md:19`；`main.py:26` | 同一章出现：9.7 表 `/login`；示例 A `POST /login`；示例 B `POST /api/login`（已写第 13–16 用 `/login`、第 19 章才冻结）；实操 9-1 验收 `/api/login`。冻结允许 B 用 `/api/login` 且必须标明——B 的标签合格（`:499`），但 9.7 表没有同等标签，学生仍会把三套路径背成 MiniShop 现在的契约。 | 旧审计只记「示例 B 已标明」。 |
| S4-09-03 | 低 | `:510-514` vs 实操 9-1 / PRD `R-AUTH` | 示例 B 成功响应只有 JSON token，无 `Set-Cookie`。正文 `:523-524` 说凭证层次可并存，但和 9-1（必须同时有 token 与 Set-Cookie）及 `evidence/http/01-login-ok.txt` 不同构。 | 旧审查把 B 当教学结构即可。 |
| S4-09-04 | 低 | `:548`、`:786-789` | 工作实战假设 MiniShop「尚未提供可运行服务」；可运行性声明审查用的是**非 MiniShop**教学 HTTP。仓库里 MiniShop 与 9-1 已经可跑（`practice/09-http-observe/validation/latest.json`：`POST /api/login`、200、token、Set-Cookie）。双运行时诚实，但「完成本章不必启动服务」和「必须跑 9-1」打架。 | 旧审查把教学服务当加分项。 |

冻结项（第 9 章）结论：safe ≠ 机密性、cacheable ≠ safe、PATCH=RFC 5789、登录≠两笔订单、302/303/307、回环例外——**通过**。实操 9-1 对 v1.0 `/api/` 登录——**通过**。

测验：`quizzes/stage-3-web.md` Q4 禁「GET 不安全 POST 安全」；Q9 要 token + Set-Cookie 并标第 19 章冻结。Q4 答案写 safe「不应产生除取信息以外的服务器副作用」，比正文/RFC「客户端不请求改变状态、写日志不破定义」（`:287`、`:678`）略硬，列为可接受风险。

### 第 11 章

| ID | 级别 | 位置 | 问题 | 证据对照 |
| --- | --- | --- | --- | --- |
| S4-11-01 | 中 | `11-linux.md:42-46` | 场景：「MiniShop 测试环境提交购物车数量 11 后……Network 里 `POST` 返回 `500`」。 | PRD `R-CART-10`；`server.py` qty 超库存 → 400 + 日志 `inventory reject`；`evidence/http/05-cart-qty-11.txt` 为 `HTTP 400`；实操 11-1 验收 HTTP 400。开篇把主案例事实写错。磁盘满导致 500（`:353`）可以另说，不应绑在 qty=11 上。 |
| S4-11-02 | 中 | `:208-213`、`:227-230` vs evidence | 教学日志：`2026-09-08 13:01:05 ERROR inventory reject sku=SKU-DEMO-001 stock=10 qty=11`，并示范 `grep ERROR`。 | `evidence/logs/app-sample.log` 与 `evidence/linux/grep-app-log.txt`：同 sku/qty，级别是 **`INFO`**，时间是 `23:43:54` / `2026-09-09 10:17` / `14:30`。正文声明「不是正式日志格式」（`:208`），工作实战摘录（`:490-493`）与 evidence 一致。学生若用场景+11.5 的 `grep ERROR` 打 `app-sample.log`，inventory reject **0 命中**。 |
| S4-11-03 | 低 | `:435` vs `:441` | 散文写端口「`8080` 仍是教学占位」，命令是 `127.0.0.1:PORT/login`。v1.0 标注 `8765/api/login` 本身合格。 | 占位符不统一。 |
| S4-11-04 | 低 | 文首无阅读提示；实操在 `:458` | 第 9 章把 9-1 放在标题下；第 11 章要读完 curl 才看到 11-1。 | 结构摩擦，不是知识错误。 |

冻结项（第 11 章）结论：macOS/`free`、curl 占位 vs `8765/api/login`、不把 SQL 当硬前置（`:16`）、实操 11-1 = qty=11 后 grep inventory reject——**通过**。虚构远程 Linux——**未发现**（`:717` 明确反过来说）。

测验：`quizzes/stage-4-ops.md` Q3 `free`、Q4 curl 脱敏、Q10「Linux 不是 SQL 硬前置」与正文一致。

### 第 12 章（12 / 12A / 12B）

| ID | 级别 | 位置 | 问题 |
| --- | --- | --- | --- |
| S4-12-01 | 中 | `12b-sql-write-and-minishop.md:24` vs `practice/12-sql-cross-check/main.py:21-22` | 场景：「页面把数量改成 11，接口返回 400……实操 12-1 就是这条交叉验证。」12-1 实际是 `QTY = 2`，断言 API 与 JOIN 都是 2。qty=11 不落库在 `evidence/sql/seed-join.txt:6-11` 与 `tests/test_api.py::test_cart_qty_11_does_not_persist`。冻结要求 12-1 是 JOIN 核对购物车——**脚本对、场景句不对**。 |
| S4-12-02 | 中 | `12b:26-35`（12.12 INSERT） | 冻结问「写操作安全警告是否每次都在」。章首（`:9`）、12.13（`:43-54`）、DROP/TRUNCATE（`:113`）、工作实战（`:193`）、常见错误 1/4 都有「授权 + WHERE + 先 SELECT」。**第一条写语句 INSERT 节没有重复「只在授权教学库」**，也没有插入前 SELECT 范围（只有插入后 SELECT、事后回滚/按主键删）。质量标准 §六点名 UPDATE/DELETE/DROP/TRUNCATE，INSERT 同样改库。 |
| S4-12-03 | 中 | `12a:366-368`；12A/12B 均无「进入下一章的自测门槛」 | 12A 工作实战三句话指向 `sql-check.md` 与 `seed-join.txt`，无提交模板、无命令、无验收。12B 才有完整 SQL 验证包。12A 检查清单仅 3 条（`:448-452`）。作为「一章的上半」，结构不完整。 |
| S4-12-04 | 低 | `12b:297-303` 练习 7–8 | LEFT JOIN 行复制、无 `ORDER BY` 的 `LIMIT 1` 是 12A 内容，拆章后留在写操作节。 |
| S4-12-05 | 低 | `12a:458-465` | 可运行性只指向 v1.0 JOIN 证据；「审查时已插入并查询」（`:129`）的教学库结果没有独立 evidence 文件（数字与 INSERT 算术一致，见下）。参考资料仅「SQLite 文档」无 URL。 |

冻结项（第 12 章）结论：改数仅授权库、先 SELECT（UPDATE/DELETE 路径）、SQLite 无 TRUNCATE、ROLLBACK 因引擎而异、Linux 非硬前置、`13800138002` ≠ `13800138099`、声称插入则正文有 INSERT——**通过**。实操 12-1 JOIN 核对——**脚本通过，场景描述未通过**。

12.4 教学数据与「审查结果」自洽（未重跑 sqlite，高确信来自 INSERT 原文）：

- users 3 行，`display_name` 仅用户 3 为 NULL → `COUNT(*)=3`、`COUNT(display_name)=2`
- cart qty 1+2+1=4；stock 10/5/3 → AVG=6.0、MIN=3、MAX=10
- Tester A JOIN：SKU-DEMO-001 qty 1 stock 10、SKU-DEMO-002 qty 2 stock 5，与 `evidence/sql/seed-join.txt` 第一节同构
- `qty > stock` 零行；事务内改 11 标明是对照（`12b:177`），未冒充 MiniShop 缺陷

测验：`stage-4-ops.md` Q6 先 SELECT、Q7 种子两行、Q8 无 TRUNCATE、Q9 qty=11 不得为 11、Q10 C 只在授权库——与正文/PRD 一致。

---

## 结构简化机会

### 第 9 章：应该拆，judo 是「语义」和「报文实验」分开

现在的阅读提示已经是半套 judo，但文件仍是一条必须线性读完的 13 节。建议比照 8A/8B、12A/12B：

| 建议文件 | 留下什么 | 一句话核心 |
| --- | --- | --- |
| 9A 网络地图与 HTTP 语义 | 9.1–9.6 地图（标 ⭐ 可略读）+ 9.8–9.9 safe/幂等/cacheable/GET-POST | GET/POST 不按「安不安全」划分 |
| 9B 报文观察 | 9.7 四格 + 9.10–9.13 状态码/头/体/登录报文 + 实操 9-1 + 观察记录 | HTTP 观察是方法、路径、头、体 |

即使不拆文件，最小改动是把 9-1 和「四格」提前到 9.7 之后，9.10–9.12 改为查阅表，并给 9.7 的 `/login` 加上与示例 B 同级的「教学路径，第 13–16 章 `/login`，v1.0 第 19 章 `/api/login`」标签。

**不要**把实操 9-1 改回 `/login`：冻结明确要求 9-1 对 v1.0 `/api/` 登录。要改的是正文标签，不是脚本。

### 第 11 章：不必硬拆，但场景必须改成 400

733 行作为 GNU/BSD 命令目录可以保留。若要减轻：11.1–11.4 目录操作压缩，把 11-1（qty=11 → grep）挪到 11.5/11.6 之后立刻做，curl 仍放后。开篇改为「Network 里是 400，页面只有失败文案，所以要上日志」——与 11-1、evidence 同一条故事。教学日志级别改成 `INFO`，或明确「下面是虚构的 ERROR 级别练习，仓库样本是 INFO」。

### 第 12 章：拆法正确，12A 要补齐「半章」该有的练习入口

12 枢纽 + 12A 查询 + 12B 写操作是对的，避免还没 SELECT 就 DELETE。缺口是 12A 不像 8A/9/11 那样能独立交作业：

- 12A 工作实战补：打开 `sql-check.md`、对照 `seed-join.txt` 第一节、写出 Tester A 两行 JOIN（书面即可，不必再造可运行脚本）。
- 12A 加自测门槛（能写 WHERE + JOIN + `IS NULL` 再进 12B）。
- 12B 场景改成「12-1 核对合法 qty=2；qty=11 不落库看 evidence/pytest」，练习 7–8 移回 12A。
- 12.12 INSERT 块前加一句：仅授权教学库；插入前 SELECT 是否已有该行；外键打开。

---

## 可接受风险

1. **第 19 章前的双基线。** 第 9 章示例 A 的 `shop.example.test` + `/login` + 302，和第 13–16 章教学 `/login`，与 v1.0 `127.0.0.1:8765/api/login` 并存——只要每次出现都标明，这是课程设计税，不是新错误。当前缺口只在 9.7 表。
2. **第 9 章审查用教学 HTTP（HTTP/1.0 BaseHTTP）不是 MiniShop。** 可运行性已披露（`:789`）。`evidence/http/01-login-ok.txt` 才是 MiniShop 摘录。不要把两套 curl 合成「已在 Linux 服务器验证 MiniShop」。
3. **第 11 章 `evidence/linux/` 是作者 macOS 本机。** `df.txt`、`pwd` 含 `/Users/plong/…`。正文说「本机真实输出（2026-09-09）」合格；学生复制 pwd 当标准答案会失败——应继续强调「贴你自己的输出」。
4. **12.4 教学库无 `password_hash`/`role`/`UNIQUE(user_id,product_id)`，与 MiniShop schema 不同。** 已禁止用 002 对 v1.0 管理员。学生若把 12.12 INSERT 打进 MiniShop 主库，行为可能不同。工作实战指定 `~/minishop-sql-lab.sqlite` + 12.4 建表，风险可控。
5. **12A「审查结果」无独立 evidence 文件。** 数字与 INSERT 一致；v1.0 只保证 `seed-join.txt`。不把「审查中 FOREIGN KEY constraint failed」写成仓库可下载证据。
6. **`exercises/` 无 chapter-09/11/12 已填包。** 各章建议保存路径是读者作业，STATUS 标 Reader exercise，不虚构完成。
7. **阶段测验 Q4 对 safe 的「无副作用」略绝对。** 正文已纠正；测验改词即可，不挡发布。
8. **QUERY（RFC 10008，2026-06）塞在 ⭐⭐⭐ 方法表旁。** 技术正确，初级可标 ⭐；不构成错误。

---

## 交接

### 冻结口径速查（本轮）

| 口径 | 结果 | 锚点 |
| --- | --- | --- |
| safe / idempotent / cacheable 分开；禁止 safe=可缓存 | 通过 | `09:286-287`、`09:341` |
| PATCH = RFC 5789，不在 9110 表 | 通过 | `09:302` |
| 禁止 GET 不安全 POST 安全 | 通过 | `09:16`、`09:318-322`、`09:608-610`、测验 Q4 |
| 禁止用「POST 一定两笔订单」讲登录 | 通过 | `09:306` |
| 302 不一定变 GET；区分 303/307 | 通过 | `09:387-389`、`09:495` |
| 127.0.0.1 不加「绝对不会出网」 | 通过 | `09:98` 仅回环/本机例外 |
| 示例 B `/api/login` 标明第 19 章才冻结；13–16 为 `/login` | 通过（9.7 表除外） | `09:499`；缺口 `09:257` |
| 实操 9-1 → v1.0 `/api/login` | 通过 | `practice/09-http-observe/main.py:26` |
| macOS 无 GNU `free` 同样输出 | 通过 | `11:351`、`11:557` |
| curl 占位 PORT/login，标明 v1.0 `8765/api/login` | 通过 | `11:415`、`11:435` |
| 实操 11-1 → qty=11 后 grep inventory reject | 通过 | `practice/11-log-grep/main.py:36-50` |
| 不虚构真实 Linux 主机 | 通过 | `11:715-717`；df/pwd 为本机 |
| Linux 非第 12 章硬前置 | 通过 | `11:16`、`12a:23`、测验 Q10 |
| 改数仅授权库；先 SELECT | 通过（INSERT 节弱） | `12b:9`、`12b:43-54` |
| SQLite 无 TRUNCATE；ROLLBACK 因引擎而异 | 通过 | `12b:113`、`12b:255-257` |
| 教学第三账号 `13800138002` ≠ 管理员 `13800138099` | 通过 | `12a:125`、`12a:382` |
| 声称插入则正文有 INSERT | 通过 | `12a:109-122` |
| 实操 12-1 → SQL JOIN 核对购物车 | 脚本通过；场景句失败 | `12-sql-cross-check/main.py` vs `12b:24` |

### 建议修正顺序（仍不改源文件，只给下一手）

1. **第 11 章场景 500 → 400**，教学日志 `ERROR` → 与 `app-sample.log` 同为 `INFO`，或加「样本是 INFO，不要 grep ERROR」。这是唯一会让学生按正文操作打空的中项。
2. **第 12B 场景去掉「12-1 就是 qty=11」**；12.12 补授权句。用来冲 95。
3. **第 9 章 9.7 表给 `/login` 贴标签**；决定拆 9A/9B 或至少把四格/9-1 前移。不改 9-1 路径。
4. 12A 补工作实战与自测门槛；12B 练习 7–8 回 12A。

### 不要做的事

- 不要为了「统一路径」把实操 9-1 改成 `/login`。
- 不要把 `evidence/linux/df.txt` 改写成假 Linux `free -h` 去「补证据」。
- 不要删 12.4 的 `13800138002`；保留 NULL 练习，继续写清 ≠ `13800138099`。
- 不要把 12-1 改成 qty=11：11-1 已经覆盖拒绝路径，12-1 覆盖合法写入交叉验证。

### 评分置信

- 冻结项通过/失败、500 vs 400、日志 INFO vs ERROR、12-1 qty=2 vs 场景 qty=11、9.7 路径未标注：**高**
- 分值 ±1（尤其第 12 章 93 相对 95、第 9 章结构 3 还是 4）：**中高**。即使第 12 章各项再松 1 分，INSERT 警告与 12A 结构仍会挡住稳定的 95。
