# 第 19 章教学审查

- 文件：`chapters/19-minishop-project.md`；对照 `practice/19-project-pack/README.md`、`project/minishop/README.md`、`docs/PRD.md`、`chapters/assets/diagrams/ch19-*.html`、邻章 18 末 / 20 开篇
- 学习顺序位置：上一章 18（了解即可） / 下一章 20（面试表达）
- 阶梯 7/10
- 清晰 8/10
- 实操与图 7/10
- 小计 22/30
- 发布建议：修正后

## 总判

能跟上收成主线：pytest 37/1、Postman 15、BUG-001 开放、非范围都和仓库对得上，不是再开一堂接口课。缺的是边看边做和跳转：19-1 只锁仓库，工作实战在文末，第 11/12/14 章几乎不给可跳。会逛目录，不等于会指给人看。

## 阶梯

- 章内坡度：开篇用「面试官要看项目」带出五件可指之物，前 50 行是收口而不是五条新定义，19.1 先给启动命令，方向对。之后 19.2～19.10 变成仓库导览，真正五步必做堆在 19.10 之后；读者会先读完十节指针再动手。练习 1～9 识别、练习 10 口述，坡度合理。整章全 ⭐⭐⭐，没有「可跳」层。
- 相对上一章：第 18 章末预告「收成可展示材料、个人实践」，第 19 章开篇接「前面各章碎片还缺一次收成」，且不要求点 JMeter GUI / 加压。承接干净，没有把了解章写成工具手册。
- 相对下一章：第 20 章开篇 40 行直接用 PRD、qty=10/11、BUG-001、37/1，不新造技术规则。本章「面试角度」只挖 MiniShop，没有把第 20 章的自我介绍/行为面试讲完；五段结构是全书统一口径，可保留。
- 星级是否诚实：章标题 ⭐⭐⭐ 作为核心收成诚实。19.6 Linux/SQL、19.7 Postman 也标必须掌握，与「第 11 与 12 并列、互不为硬前置」以及第 14 章已有 14.6.1 导入不对称。前置写「已完成第 1～18 章中与本项目相关的技能」，既像假设全做完，又没有跳转表。反过来：第 16 章实操 16-1 已经跑过 37/1，本章不说明「已跑通可只指着 xfail 讲」，等于假设读者没做前面实操、再逛一遍。

## 清晰

- 一句话核心是否真能当过滤器：能。「把观察、判定、证据收成一套能指给人看的项目」挡住了「再讲一遍接口」和「编公司/状态机」。19.1 工作台图、19.2 PRD 摘要、常见错误 1/3/4、19.10 简历可写/不可写，都在用这句过滤。19.7 只是把审查 curl 结果报一遍，没有再教怎么写 `pm.test`，仍算收成不是接口课。
- 术语/例子问题：`/api/`、qty=10 允许 / 11 拒绝、订单无 `status`、Cookie+Bearer 同时下发、教学 `/login` 标明「第 13～18 章约定、不要当可抄示例」、验证码明确不要写进本项目——与冻结口径一致。数字抽查见下，无 P0。弱在 19.3 / 19.4：学习目标要求「解释为什么裁掉状态机」「按计划执行 P0/P1」，正文却几乎只有路径，没有「打开文件后第一眼看哪三行」。

数字核对（正文 vs 仓库，本轮已修过的 9→15 不再算债）：

| 项 | 正文 | 仓库 | 结论 |
| --- | --- | --- | --- |
| pytest | 37 passed, 1 xfailed；收集 38 | `tests/` 收集 38（`test_api` 23 含 8 组 qty + 1 xfail，`test_register` 11，`test_qty_rule` 4）；`evidence/pytest-output.txt` 同 | 一致 |
| xfail / BUG-001 | `test_empty_keyword_should_not_return_all`，`strict=True`，仍开放 | `bugs/BUG-001.md`「v1.0 保持开放」；19-1 用「保持开放 / 不在报告里写成已修复」判定 | 一致 |
| Postman | **15** 个请求：3 注册、2 搜索、3 登录、3 改数量、创建订单、无凭证、2 条越权 | 集合 `item` 恰好这 15 个；环境 `password`/`token` 初始空；第 14 章 14.6.1 已按 15 写 | 一致 |
| 不做清单 | 支付、物流、优惠券、订单状态机、生产 HTTPS | PRD 非范围另列退款、评价、短信验证码、正式域名；README 另列 HTTPS 终止、生产部署 | 关键项一致；清单长度略不等，P2 |

## 实操与图

- 实操类型与验收：章内工作实战有五条必做、执行记录模板、完成标准，产出物路径 `exercises/chapter-19-minishop-run.md`（`exercises/README.md` 已点名，目录里没有现成空文件，读者自建即可）。可运行实操 19-1（`practice/README.md` 标 ✅）命令/验收齐全，但练的是「仓库文件还在 + pytest 仍是 37/1」，**一次浏览器都不用开也能过**。门槛 2～4 才要求启动、指着 BUG-001、写执行记录，和 19-1 脱节。19.8 又写 `python3 -m pytest -q`，不走 19.1 的 `run.py test` / `.venv`，未 `setup` 的读者会在这里卡住。
- 现有图是否讲难点：`ch19-workbench`（能跑 + 契约 + 证据）对准一句话核心，标题能当教学句。`ch19-qty-rule`（10 允许 / 11 拒绝）对准双基线，必要，但是第 5/13/16 章已经练过的尺子，不是本章新难点。页面截图 01～05、07 和 pytest-html 09 能当证据墙；同目录已有 `06-register.png`、`08-network-log.png` 正文未挂。
- 缺图的概念（最多 3 个，要具体到节）：
  1. 19.5～19.6 同一条库存规则的四通道交叉（页面 / `POST /api/cart/items` / SQL / `logs/app.log`）——学习目标写了，没有图。
  2. 19.8 passed vs xfailed vs 真失败（`strict=True` 修了却不改用例会变红）——有文字和报告截图，缺并排盒子。
  3. 19.3 范围裁剪（PRD 非范围 → 用例不写状态名 → 简历不写状态机）——只有两句散文。

## 问题清单

| ID | 级别 | 位置 | 问题 | 建议改法 |
| --- | --- | --- | --- | --- |
| P19-01 | P1 | 前置知识；19.6；19.7；19.8 | 假设 1～18 相关技能全做完，Linux/SQL/DevTools 无跳转；又把 16-1 已跑过的 37/1 当新步骤重来。Postman 仅有「见 14.6.1」一处跳转。 | 加「已做 / 没做」跳转表；已跑通 16-1 则只解释 xfail；没做 12 则对照种子表、不强制手写 JOIN。 |
| P19-02 | P1 | MiniShop 工作实战；`practice/19-project-pack/` | 一句话核心是「能指给人看」，19-1 只验收作者仓库没被改坏。读者可不开首页、不复现 BUG-001、不写介绍就拿到 ✅。 | 正文写明 19-1 只锁基线；五步必做以执行记录为准；19-1 README 加一句「过关不等于能面试讲解」。 |
| P19-03 | P1 | 19.1 之后到工作实战 | 动手五步在文末。导览中 19.3/19.4 只有路径，学习目标里的「解释裁剪」「按计划执行」没有第一眼锚点。 | 19.1 末先给五步路线；19.3/19.4 各写「打开文件看哪几行」。 |
| P19-04 | P1 | 19.8 | 命令写成系统 `python3 -m pytest -q`，与 19.1 / `run.py test`（优先 `.venv`）不一致，未 setup 会直接 `No module named pytest`。 | 与 19.1 统一为 `python3 run.py test`，必要时再写「已激活 venv 才用 -m pytest」。 |
| P19-05 | P2 | 19.5～19.6；示意图 | 本章新难点是交叉核对，现有图停在 qty 尺子和工作台。截图跳过已存在的注册页、Network 日志。 | 补四通道图；19.5 挂 `06-register.png`，19.6 挂 `08-network-log.png`。 |
| P19-06 | P2 | 19.2 非范围 vs PRD | 正文不做清单短于 PRD（缺退款/评价/短信验证码）。不至于按错练，面试对照 PRD 时会觉得「章里没写能不能写」。 | 非范围改成「以 PRD 非范围为准」并点名支付、状态机、验证码。 |

无 P0：未见把 `/login` 当 MiniShop 可抄示例、未见验证码进工作实战、Postman 个数与 pytest 数字正确、BUG-001 仍开放。作者未点 Postman GUI 已披露，不记缺陷。

## 可落地补丁（给主审查改正文用）

### 补丁 1 — 前置改成「已做 / 没做」跳转（P19-01）

- 目标文件：`chapters/19-minishop-project.md`
- 位置：替换「## 前置知识」整节
- 拟写：

```markdown
## 前置知识

本章是收成，不新造规则。下面按「已做 / 没做」走，不要把第 11 与 12 章都当成硬前置。

| 技能 | 已做过 | 没做过（可跳操作，不能不懂结论） |
| --- | --- | --- |
| 启动与目录 | 直接 19.1 | 按 19.1 `run.py setup` / `serve` |
| pytest 37/1 | 第 16 章实操 16-1 已跑通：本节只解释 xfail=BUG-001，不必当第一次 | [16B](16b-pytest-fixtures.md)；命令用 `python3 run.py test` |
| Postman 15 请求 | 已按 14.6.1 导入：对照集合即可 | [14.6.1](14-postman.md) |
| SQL JOIN | 已做 12-1：对照 `docs/sql-check.md` | 用正文种子表两行说明即可，不强制手写 SQL |
| DevTools / grep | 已做第 10 / 11 章 | 看 `evidence/http/` 与正文日志两行；不要用本机 `free` |
| JMeter（第 18 章） | 了解即可 | 本章不加压，可整节跳过 `jmeter/` |
```

### 补丁 2 — 19.1 末先发五步，避免文末才动手（P19-03）

- 目标文件：`chapters/19-minishop-project.md`
- 位置：19.1 节末、`MINISHOP_RESET=1` 段落后
- 拟写：

```markdown
边看仓库边做（完成标准仍见文末工作实战，不要读完全章才打开浏览器）：

1. 本机 `python3 run.py serve`，浏览器打开打印出的 `MINISHOP_BASE_URL`；
2. 另开终端 `python3 run.py test`，记录 passed / xfailed（已做 16-1 则抄你上次数字并复跑一次核对）；
3. 搜索框提交空格，对照 `bugs/BUG-001.md`，确认仍开放；
4. 用 `docs/sql-check.md` 的两行种子结果，或自己跑 JOIN；
5. 把过程写入 `exercises/chapter-19-minishop-run.md`。

`python3 practice/run.py 19-1` 只检查仓库没被改坏、基线仍是 37/1。能指给人看，靠上面 5 步和执行记录。
```

### 补丁 3 — 19.3 / 19.4 给「第一眼看哪」（P19-03）

- 目标文件：`chapters/19-minishop-project.md`
- 位置：替换 19.3 第二段，并在 19.4 列表后补一段
- 拟写：

```markdown
打开 `docs/requirement-review.md`，先看这三格：①「订单有哪些状态？」→ 写入非范围；②「空搜索应怎样？」→ R-SEARCH，实现未满足所以后面才有 BUG-001；③「注册成功是否自动登录？」→ 否，201 不回 token。
所以测试工程师要做的是：裁掉的字段不要进用例和简历；已确认但实现不符的，记成开放缺陷，不要含糊。

打开 `docs/test-plan.md` 的「优先级」和「出口」两节：P0 含 qty=10/11 与注册；空搜索是 P1。再打开 `docs/test-cases.md` 摘要表，确认 TC-SEARCH-001 结果是失败/BUG-001，且没有「待支付」列。商品列表由 pytest 覆盖、用例表不单列（计划里已写）。
```

### 补丁 4 — 工作实战与 19-1 对齐（P19-02）

- 目标文件：`chapters/19-minishop-project.md`（工作实战「必做」前）；`practice/19-project-pack/README.md`（「验收条件」后）
- 位置：见上
- 拟写（章内，插在「必做：」前）：

```markdown
类型：✅ 启动 + 跑自动化 + 书面执行记录。可运行检查 `python3 practice/run.py 19-1` 只锁 PRD/BUG-001/OpenAPI/Postman 文件和 37/1，**不**代替下面 5 步。
面试时要能指着仓库讲：需求在哪、测了什么、缺陷为何仍开、数字从哪来。
```

- 拟写（`practice/19-project-pack/README.md` 验收条件后）：

```markdown
过关不等于能面试讲解。回到第 19 章工作实战：启动首页、亲手复现 BUG-001、对照种子 JOIN、把执行记录写到 `exercises/chapter-19-minishop-run.md`。
若你改过 `tests/` 或修了空搜索，19-1 会失败——那是基线变了，先对照 `project/minishop/README.md`，不要为了绿灯关掉 xfail。
```

### 补丁 5 — 交叉核对图（P19-05）

- 目标文件：新图 `chapters/assets/diagrams/ch19-cross-check.html`（及同名 png）；插入 19.6 节首、Linux 日志示例前
- 图需求：
  - 文件名建议：`ch19-cross-check`
  - 一句话命题：同一条「qty=11 拒绝」要在页面、接口、SQL、日志对上，对不上就还不能结案
  - 要画的盒子：① 页面购物车填 11 → 提示拒绝；② `POST /api/cart/items` → 400 `qty exceeds stock`；③ SQL 该 SKU 数量不得变成 11；④ `logs/app.log` 有 `inventory reject ... qty=11`；⑤ 底注：只查本机教学库；pytest 绿不能代替 ①
- 插在正文的一句：`![同一条库存规则要在四通道对上](assets/diagrams/ch19-cross-check.png)`
- 顺手：19.5 截图列表补 `assets/06-register.png`（标题：首页有注册入口，避免「范围有注册、页面无入口」）；19.6 DevTools 句后补 `assets/08-network-log.png`（第一帧：看 `POST /api/login` 的状态码和 `Set-Cookie`，不要把 token 存进仓库）。

### 补丁 6 — pytest 命令与非范围对齐（P19-04 / P19-06）

- 目标文件：`chapters/19-minishop-project.md`
- 位置：19.8 命令块；19.2「非范围」句
- 拟写（替换 19.8 命令）：

```markdown
```bash
cd project/minishop
python3 run.py test       # 使用 .venv 里的 pytest；不要先敲系统 python3 -m pytest
```

审查摘要（pytest 9.1.1 / requests 2.34.2）：

```text
37 passed, 1 xfailed
```
```

- 拟写（替换 19.2 非范围句）：

```markdown
非范围以 `docs/PRD.md` 为准：支付、退款、物流、优惠券、评价、订单状态机、正式域名/HTTPS、短信验证码。第 4、5 章用验证码练设计，不要写进本项目用例或简历。
```
