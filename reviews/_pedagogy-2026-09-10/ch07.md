# 第 7 章教学审查

- 文件：`chapters/07-web-basics.md`（练习页 `chapters/assets/07-html-lab.html`、`07-html-combo.html`；图 `chapters/assets/diagrams/ch07-*.html`）
- 学习顺序位置：上一章 03 / 下一章 04（先 04A）
- 阶梯 /10：7
- 清晰 /10：8
- 实操与图 /10：7
- 小计 /30：22
- 发布建议：修正后

## 总判

零基础读者跟得上「页面不是整个系统」这句话，HTTP 报文也没有被提前讲完。卡住点在开篇就把六段 URL 和术语清单一次倒完，真正能点的页面要到 7.8；工作实战一边禁止虚构商品列表，一边用 v1.0 不存在的「加入购物车」做故障题。先改实战对象与 7.4 堆砌。

## 阶梯

- 章内坡度：7.1 ⭐⭐ → 访问链路 / URL / DNS / 前后端 ⭐⭐⭐ → 7.9 渲染 ⭐⭐，方向对。动手（打开 combo / 登录页）排在约 300 行概念之后，前半像词典。学习目标 9 条在前 30 行点名 scheme/host/port/path/query/fragment、DNS、前后端、HTML/CSS/JS，开篇不是「一个最小例子」。
- 相对上一章：第 3 章末预告正确（先 07 再 04）。本章前置只罗列「已学分类」，场景导入没有接上「系统测试常在浏览器里发生」。从六条分类轴跳到完整 URL，中间缺一句为什么现在要看 Web。
- 相对下一章：预告指向 04A，没有把 PRD / 歧义 / 测试点讲完。HTTP 方法、状态码、Header/Body、DevTools 都标明第 9 / 10 章，克制合格。7.2 表直接写「页面返回 404」、面试答「看控制台 / 响应状态」，零基础会以为本章就要会这些。
- 星级是否诚实：7.1、7.9 降星诚实。7.4 把 domain / host / hostname 的标准差异标成 ⭐⭐⭐，对测试入门过重；正文自己也说「工具用词略有差异」。

## 清晰

- 一句话核心是否真能当过滤器：能。「页面只是观察通道」可以筛掉「看见就判前端」。7.2 现象表、7.7 负数价格、错误 5 都在用它。7.1（网页 / 网站 / Web 应用）离这句稍远，但不伤主线。
- 术语/例子问题：URL、DNS、前后端、HTML 职责都有类比 + 反例。7.4 是定义堆砌：host / hostname / domain 绕一圈，免责声明比可执行区别长。开篇示例 URL 六段齐全，本机实战却是 `http://127.0.0.1:8765/`（无域名、无 query、无 fragment），读者会对不上「刚才学的格子」。示意图 `ch07-client-server` / `ch07-frontend-backend` 写出 JSON、SQLite、`/api/login`，正文 7.6～7.7 还没教这些词。

## 实操与图

- 实操类型与验收：章内书面实战（`practice/README.md` 编号 7-1 📖，正文未写类型）。产出 `exercises/chapter-07-web-observation.md`，完成标准能量化。缺启动命令和第一帧：学习顺序到此只跑过 `practice/run.py 1-1`，`python3 run.py serve` 出现在第 10 / 13 / 19 章。备选 `07-html-lab.html` 能避免完全卡住。任务三「加入购物车按钮无反应 / 商品数量显示错误」与「不要虚构商品列表」打架；v1.0 登录后按钮是「更新数量」「创建订单」，「加入购物车」只在 combo 教学页。combo / lab 已与正文 HTML 对齐（上一轮 ISS-07-02/03 的文件问题已修）。
- 现有图是否讲难点：五张图命题清楚，alt 能当教学句（DNS「通讯录不保证店开着」、URL「拆开才能写到格子上」）。7.9 mermaid 已改为 JS → 改 DOM / 绘制，不再指回「解析 HTML」。访问六步只有 mermaid，没有「故障落在哪一格」的图，而那正是核心句。
- 缺图的概念（最多 3 个，要具体到节）：
  1. 7.2 测试工程师如何使用这条链路：现象落在地址 / DNS / 服务 / 资源 / 呈现哪一格；
  2. 7.4 URL vs 域名 vs IP（不要画 host/hostname 三分法庭）；
  3. 7.9 / 练习 6：查看源代码 ≠ 运行后的 DOM。

## 问题清单

| ID | 级别 | 位置 | 问题 | 建议改法 |
| --- | --- | --- | --- | --- |
| P1-1 | P1 | MiniShop 工作实战 · 任务三 | 刚写「观察登录页、不要虚构商品列表」，接着用商品页 / 加入购物车 / 数量错误。v1.0 无「加入购物车」，按 MiniShop 练会找错控件或误报缺陷。 | 任务三改成登录页 / lab / combo 上能看见的现象；购物车只作假想题并指向 combo。 |
| P1-2 | P1 | MiniShop 工作实战开段 | 点名 `http://127.0.0.1:8765/`，无启动命令、无第一帧（应看到登录+注册，对照 `01-login.png`）。未标 📖 / 7-1。 | 给 serve 命令或「先用 lab」；写第一帧；类型 📖。 |
| P1-3 | P1 | 7.4 Domain、Host、Hostname 与 IP ⭐⭐⭐ | 测试员此刻只需 URL / 名字 / 号码。三词分叉 +「工具口径不同」是定义堆砌，星级不诚实。 | 降为 ⭐⭐，改三分表；host=URL 里「去哪」一格即可。 |
| P1-4 | P1 | 学习目标 + 场景导入 | 开篇一次倒完本章术语；示例 URL 比本机实战地址复杂一整截。动手页在 7.8。 | 场景用短地址承接第 3 章；六段拆解放 7.3；combo 提前成「先点一下」。 |
| P2-1 | P2 | 7.2 现象表；面试问题 2 | 「404」「控制台」当已会技能。 | 404 加口语；控制台标「第 10 章再打开」。 |
| P2-2 | P2 | `ch07-client-server.html` / `ch07-frontend-backend.html` | 图里 JSON、SQLite、qty=11 超前于正文。qty=11 可留，JSON/SQLite 改成「数据和规则」。 | 图文同期；正文给一句「名字以后在第 9、12 章展开」。 |

无 P0：`/login` 未当可抄路径，工作实战无验证码，无 Postman 个数。combo 与正文 `<article>` / 价格 / 按钮文案已对齐。

## 可落地补丁（给主审查改正文用）

### 1. 场景导入改短地址，并接上第 3 章

- 目标文件：`chapters/07-web-basics.md`
- 位置：「场景导入：输入网址以后发生了什么？」整节开头（现完整六段 URL + mermaid 可保留为「学完 7.3 再回看」）
- 拟写：

```markdown
第 3 章说：系统级功能测试常常是打开浏览器去点 MiniShop。
要点得下去，先承认一件事：你看见的页面只是整条链路的最后一截，不是整个被测系统。

先看本机登录页这个短地址（项目还没启动也没关系，7.8 有不连服务器的练习页）：

http://127.0.0.1:8765/

此刻只需三个格子：`http` 怎么走，`127.0.0.1` 去哪台机器，`8765` 敲哪扇门，末尾 `/` 是默认页面。
带搜索词、非默认端口和 `#锚点` 的完整拆解，放到 7.3 再练。
```

### 2. 7.4 降星，改成测试员三分法

- 目标文件：`chapters/07-web-basics.md`
- 位置：替换「7.4 Domain、Host、Hostname 与 IP ⭐⭐⭐」正文（保留 `.test` 与「禁止一对一」）
- 拟写：

```markdown
## 7.4 URL、名字和号码 ⭐⭐

入门先记三样。domain / host / hostname 在不同工具里用词不完全一样，不必先打官司。

| 你看见的 | 它是什么 | 打不开时先查 |
| --- | --- | --- |
| 完整 URL | 怎么走 + 去哪 + 哪份资源 | 整串有没有抄错、环境对不对 |
| 域名 / 主机名 | 给人看的名字，如 `shop.example.test` | DNS、Hosts、证书名字 |
| IP 地址 | 给机器看的号码 | 这台地址上有没有服务在听 |

URL 里的 host 就是「去哪」这一格，可以是名字，也可以直接写 IP。
名字能解析，只说明通讯录里有号码，不说明店开着——下一节 DNS 专门练这句。
禁止把「一个域名永远对应一台服务器」当成规则。
```

### 3. 工作实战：类型、第一帧、任务三对齐 v1.0 / combo

- 目标文件：`chapters/07-web-basics.md`
- 位置：「MiniShop 工作实战：建立页面观察记录」开段 + 任务二 + 任务三 + 完成标准
- 拟写：

```markdown
类型：📖 书面实操（编号 7-1）。产出：`exercises/chapter-07-web-observation.md`。

优先打开练习页 `assets/07-html-lab.html`（双击即可，不连服务器）。
若要看真 MiniShop：在仓库根目录执行 `python3 project/minishop/run.py serve`，
浏览器打开打印出的地址（默认 `http://127.0.0.1:8765/`）。
第一帧应看到「登录」和「注册」两个表单，对照 `assets/01-login.png`。
未登录不要往下编商品列表。combo 页 `assets/07-html-combo.html` 用来观察「点按钮只改文字」。

任务二在 lab / 登录页上认 HTML/CSS/按钮；再打开 combo，记录点击前「加入购物车」、点击后「已加入购物车」。

任务三（现象题，不必在 MiniShop 复现）：
1. 提示无法解析主机名（短地址是 IP 时，说明为什么这题对 127.0.0.1 套不上）；
2. 文字还在，样式没了；
3. combo 页按钮能看见，点击后文字不变；
4. 登录页能打开，点「登录」后只出现本页提示、没有商品区。
不要把 v1.0 的「更新数量 / 创建订单」写成「加入购物车」。
```

完成标准加一条：combo 点击前后文案有记录。

### 4. 7.2 旁补「故障落在哪一格」图

- 目标文件：新图 `chapters/assets/diagrams/ch07-observation-channel.html`（png 同名）；插在「测试工程师如何使用这条链路？」表前
- 图需求：
  - 文件名建议：`ch07-observation-channel`
  - 一句话命题：白屏、打不开、没样式、按钮没反应，坏在不同格子；页面只是最后一格
  - 要画的盒子（左→右）：① 地址栏（URL）② 名字变号码（DNS）③ 门牌后的服务 ④ HTML/CSS/JS 资源 ⑤ 你看见的页面
  - 每格下一句反例：改密码救不了 ②；服务挂了不是「前端没写页面」；⑤ 红了不能直接判前端
- 表内「页面返回 404」改为「页面提示找不到（常见 404，状态码第 9 章再记）」

### 5. 7.4 旁补 URL / 名字 / 号码图

- 目标文件：新图 `chapters/assets/diagrams/ch07-url-name-ip.html`；插在补丁 2 的表后
- 图需求：
  - 文件名建议：`ch07-url-name-ip`
  - 一句话命题：域名不是完整网址；IP 也不是完整网址
  - 要画的盒子：① 完整 URL 长条（标出 host 那一格）② 域名 `shop.example.test` ③ IP `203.0.113.10`（写明示例网段）④ DNS 箭头「名字 → 号码」
  - 脚注：本机实战常用 `http://127.0.0.1:8765/`，没有域名，host 直接是 IP；教学拆解仍可用 `shop.example.test`

### 6. 7.8 把 combo 变成必做的最小动作，并管住超前词

- 目标文件：`chapters/07-web-basics.md` 7.8 末段；`chapters/assets/diagrams/ch07-client-server.html`、`ch07-frontend-backend.html` 文案
- 位置：「组合页已放入仓库」那段，改为带验收的三步；图中 JSON/SQLite 改口
- 拟写（正文）：

```markdown
现在就打开 `assets/07-html-combo.html`（不必等 MiniShop）：
1. 看到标题、价格、按钮 → 结构在（HTML）；
2. 卡片有边框 → 装修在（CSS）；
3. 点击后按钮变成「已加入购物车」，刷新又回去 → 只是本页脚本，没有后厨账本。

验收：你能向别人说出这三步。不要求改代码。JavaScript 示例只供对照，不会写也可以往下读。
```

- 图改口：后厨盒子写「校验密码、拒绝超量、记下库存」，不要出现 `JSON` / `SQLite`；`/api/login` 可留，加「这是门牌上的路径名，报文第 9 章再拆」。
