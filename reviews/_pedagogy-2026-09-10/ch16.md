# 第 16 章教学审查

- 文件：`chapters/16-pytest.md`、`chapters/16a-pytest-basics.md`、`chapters/16b-pytest-fixtures.md`；实操 `practice/16-pytest-regression/README.md`；图 `chapters/assets/diagrams/ch16-*.html`
- 学习顺序位置：上一章 15（15B 收尾） / 下一章 17
- 阶梯 8/10
- 清晰 8/10
- 实操与图 8/10
- 小计 24/30
- 发布建议：可发布

## 总判

读者跟得上收集、assert、qty 纯函数闭环。autouse 与 MiniShop 契约已对齐，旧教学服务不再可抄。卡住点是 16A 登录依赖尚未教的 fixture、16.4 未写先启动 serve、图仍写「六种 Body」。规定命令 `run.py test` 为 37 passed / 1 xfailed。

## 阶梯

- 章内坡度：16A 是 ROI → 安装 → 收集规则 → `assert` → requests；16B 才是 fixture / parametrize。qty 纯函数能单独跑通（审查：2 passed）。缺口：16.3 尚未出现装饰器就点名 `@pytest.fixture` / `parametrize`；16.5 登录函数已经吃 `base_url`，抄下来是 `fixture 'base_url' not found`（已复现）。16A 学习目标含「用 requests 发 JSON」，但第一条带 assert 的 HTTP 测试要等到 16B 的 fixture。
- 相对上一章：15B 收尾明确「本章不安装 requests / pytest」，预告 16A。16A 承接 Postman Runner vs pytest、venv 安装，不把 15 的 JSON/`type is int` 重讲一遍。合适。
- 相对下一章：16B 停在接口回归与 37/1，不把金字塔、Playwright、CI 讲完。17 开篇接「第 16 章已经能把登录、购物车数量和下单写成接口测试」。合适。
- 星级是否诚实：主体 ⭐⭐⭐ 名副其实。`package` scope 标了 ⭐ 了解；`pytest.toml` 标明入门用 ini。16A 把「登录 API」写成上半章必练，却不能独立跑，这一颗星略提前。

## 清晰

- 一句话核心是否真能当过滤器：16A「把已经明确的判定交给脚本重复执行」、16B「fixture 负责准备观察所需的前置，不负责发明判定」都能用来划掉 `assert True`、autouse 登录、以及「绿了=没缺陷」。索引页写明作业在 16B、实操 16-1。
- 术语/例子问题：
  - 残留契约：**无 P0**。全文可抄示例已是 `POST /api/login`、`qty=10`→200、购物车带 Bearer。`teach_server` / `POST /login` 只出现在「不要另造 / 不要打」里，不是可抄墙。
  - 16.6 片段 `base_url` 默认 `http://127.0.0.1:8765`，仓库 `tests/conftest.py` 却起临时端口并 `reset_seed_db` autouse。工作实战问「base_url 怎样起临时端口」，只读 16.6 会答成 8765。
  - 16.9 正文「不要复制**六个**几乎一样的函数」、图 `ch16-fixture` 写「购物车**六种** Body」，代码表实际 **8** 行（含 `wrong_type`、`zero`）。
  - 16A「完整可运行文件见 16B」不准确：16B 仍是片段，完整文件是 `project/minishop/tests/test_api.py`。

## 实操与图

- 实操类型与验收：
  - 实操 16-1：`python3 project/minishop/run.py setup` 后 `python3 practice/run.py 16-1`，验收 `37 passed` + `1 xfailed` + `validation/latest.json`。类型是「跑通并解释 xfail」，对准本章那句。
  - 工作实战：读 `conftest.py` / `test_api.py` / `test_register.py`，产出 `exercises/chapter-16-minishop-pytest.md`。规定命令是 `cd project/minishop && python3 run.py test`，**不依赖** `run.py serve`。
  - 本机核验（pytest 9.1.1）：`cd project/minishop && python3 -m pytest -q` → **37 passed, 1 xfailed**（`test_empty_keyword_should_not_return_all` / BUG-001）。
  - 正文可抄片段（登录 2 + 购物车 8 + 下单 1 + 搜索 1）对**刚启动**的 MiniShop（等价 `run.py serve`，独立库）：**12 passed**。空搜索 xfail 不在片段里。
  - 仓库 HTTP 套件对刚启动 serve、且**去掉** `reset_seed_db`：33 passed + 1 xfailed（外加 qty 纯函数 4 条即 37/1）。**同一进程再跑第二次**失败 3 条：`SKU-DEMO-003` 库存耗尽下单 400、注册 `13900001111` 变 409。正文没写「常驻 serve 不 reset，重复跑会脏」。规定路径用临时端口 + autouse reset，所以按工作实战命令能过。
- 现有图是否讲难点：`ch16-roi`（先自动什么）、`ch16-collect`（文件名/函数名）、`ch16-fixture`（fixture vs parametrize）、`ch16-no-autouse`（401 不要先登录）都对准难点，不是装饰。autouse 坑正文 + 图 + 错误 1 + 练习 5，讲清了：`token_a` 不是 autouse；`reset_seed_db` 才是 autouse，用来打回种子；单 return 字符串不会自动粘到请求上，共用 Session/Cookie 才会测脏。
- 缺图的概念（最多 3 个，要具体到节）：
  1. 16.6：8765 常驻 `serve` vs `conftest` 临时端口 + `reset_seed_db`（两扇门）。
  2. 16.4：第一帧该看见什么（另一个终端已打印 `MINISHOP_BASE_URL=...8765`）；现有 16.4 只有代码，没有「先启动」图或步骤。
  3. 不必再补 raise_for_status：一段「先看 status_code」比图清楚。

## 问题清单

| ID | 级别 | 位置 | 问题 | 建议改法 |
| --- | --- | --- | --- | --- |
| CH16-01 | P1 | 16A 16.5 | 上半章必练的登录测试依赖 16.6 `base_url`。按块复制会 `fixture not found`。又写「完整文件见 16B」，16B 仍是片段。 | 16A 用硬编码 `http://127.0.0.1:8765` 跑登录；完整文件只指向仓库 `tests/test_api.py`。 |
| CH16-02 | P1 | 16A 16.4 | `requests.get(...8765/api/products)` 看起来可直接跑。8765 未监听时是 `ConnectionError`（已复现）。未写「另一终端 `python3 project/minishop/run.py serve`」。 | 补第一帧：先 serve，再跑；或改成带 assert 的 `test_`。 |
| CH16-03 | P1 | 16B 16.6 / 工作实战 | 片段 fixture 打 8765；仓库 fixture 起临时端口并 reset。只读 16.6 答不出工作实战第 1 问。对着脏 serve 重跑仓库 HTTP 会红（库存/注册）。 | 钉死：作业命令是 `run.py test`；抄片段才需要刚启动的 serve。 |
| CH16-04 | P1 | 16B 16.9；`ch16-fixture.html` | 代码 8 组，正文「六个」、图「六种 Body」，漏 `wrong_type` / `zero`。图与正文口径打架。 | 一律改「八组」，图右侧补上类型错误和 0。 |
| CH16-05 | P2 | 16A 16.3 末 | 装饰器定义出现在任何 `@pytest.fixture` 代码之前，上半章也不用。 | 挪到 16B 16.6 开头，16A 删或改成「下节才用」。 |
| CH16-06 | P2 | `ch16-collect.html` caption / lead | 收集图的 lead 在讲 `-m pytest`，caption 提前说 autouse。16A 读者还没到 16B。 | lead 改回收规则；caption 只留 conftest 不被收集。 |

无 P0：未再把 `/login`、qty=1-only、`teach_server.py` 当可抄示例。

## 可落地补丁（给主审查改正文用）

### 1. 16A 16.4：补 serve 第一帧，让上半章 HTTP 能自己验收

目标文件：`chapters/16a-pytest-basics.md`  
位置：§16.4 代码块前

拟写：

```markdown
先另开一个终端，在仓库里启动 MiniShop（保持开着）：

```bash
cd project/minishop
python3 run.py serve
```

第一帧应看到类似 `MINISHOP_BASE_URL=http://127.0.0.1:8765`。没有这行就去打 8765，会 `ConnectionError`，不是断言失败。
下面改成可收集的测试（仍不需要 fixture；保存到自己练习目录的 `tests/test_products_get.py`）：

```python
import requests

def test_products_keyword_mouse():
    response = requests.get(
        "http://127.0.0.1:8765/api/products",
        params={"keyword": "鼠标"},
        timeout=5,
    )
    assert response.status_code == 200
    assert [it["sku"] for it in response.json()["items"]] == ["SKU-DEMO-001"]
```
```

### 2. 16A 16.5：登录示例去掉对 16B fixture 的依赖

目标文件：`chapters/16a-pytest-basics.md`  
位置：§16.5 段首 + 两个测试函数签名

拟写：把 `def test_login_ok(base_url):` / `f"{base_url}/api/login"` 改成字面量 `"http://127.0.0.1:8765/api/login"`（错误密码那条同样）。段首改成：

```markdown
下面两条可以在 16A 就跑（前提：§16.4 的 serve 仍在）。`base_url` fixture 到 16B 再抽。
完整可运行套件在仓库 `project/minishop/tests/test_api.py`，不要到 16B 正文里找一份「完整文件」。
```

### 3. 16A 16.3 → 16B 16.6：装饰器后移

目标文件：`chapters/16a-pytest-basics.md`、`chapters/16b-pytest-fixtures.md`  
位置：16A §16.3 末段「`@` 是装饰器…」整段删除；插入 16B §16.6 第一个代码块前

拟写：

```markdown
`@` 是装饰器：写在函数上一行，把函数交给别人处理。本节只用 pytest 提供的两个：`@pytest.fixture` 和下一节的 `@pytest.mark.parametrize`。不要自己实现装饰器，也不要展开 class 测试。
```

### 4. 16B 16.6：两扇门写死，工作实战不打脏 serve

目标文件：`chapters/16b-pytest-fixtures.md`  
位置：§16.6 `base_url` 片段之后、「仓库套件自己起临时端口」那句扩成下面这段

拟写：

```markdown
这里有两扇门，不要混：

1. **抄上面片段**到自己练习目录：对端是已经启动的 `python3 run.py serve`（8765）。serve 进程不重启，库不会自动打回种子；注册过的手机、卖完的 SKU 再跑会红。要干净就停掉再 `run.py serve`（默认会 reset）。
2. **仓库作业**不要设 8765、不要另写 conftest。命令只有：`cd project/minishop && python3 run.py setup && python3 run.py test`。`tests/conftest.py` 里的 `base_url` 会起**临时端口**，`reset_seed_db` 是 autouse，用来把库打回种子，不是拿来测 401 的。你现在不必会写那段 class / 线程，但要能指出来它 `yield` 的是 `http://127.0.0.1:随机端口`。
```

图需求（若改图）：文件名 `ch16-two-doors.html`；命题「抄片段打 8765，仓库套件自己起端口」；盒子：① 终端 serve 8765 ② 片段 `base_url` 读环境 ③ `run.py test` ④ 临时端口 ⑤ `reset_seed_db`。

### 5. 16.9 + `ch16-fixture.html`：六 → 八

目标文件：`chapters/16b-pytest-fixtures.md`；`chapters/assets/diagrams/ch16-fixture.html`（改完按 diagrams/README 重截 `ch16-fixture.png`）  
位置：§16.9 首段「不要复制六个几乎一样的函数」；图 lead / 右卡

拟写正文：把「六个」改成「八个」。图 lead 改「购物车八组 Body 用 parametrize」。右卡正文改：

```text
qty=1 / 10 / 11 / 缺 / null / "" / "1" / 0 各跑一遍，失败时 id 能看出是哪一行。
```

盒子：左 fixture（base_url、token_a）；右 parametrize 八行 id：`ok` `eq_stock` `over_stock` `missing` `null` `empty_str` `wrong_type` `zero`。

### 6. `ch16-collect.html`：caption 不要提前讲 autouse

目标文件：`chapters/assets/diagrams/ch16-collect.html`（重截 `ch16-collect.png`）  
位置：`.lead` 与 `.caption`

拟写：

- lead：`文件要叫 test_*.py 或 *_test.py，函数要叫 test_*。名字不像约定，pytest 会当没看见。`
- caption：`示意图：conftest.py 放公共 fixture，自己不会当测试收集。python3 -m pytest 用的是当前解释器里的那一份。`

盒子保持现有左右两卡（会收集 / 不会收集）即可。
